# ppduster research report

## Counts
- commercial summaries: 8
- repo batches ok: 20
- repos collected: 100
- platform specs: 6
- safety specs: 4
- synthesizer specs: 2

## Final spec JSON

```json
{"architecture_notes":["Engine is generic; junk knowledge lives in versioned declarative YAML packs (id, category, risk, platform, paths, globs, min_age_days, max_depth, delete_mode, default_enabled, report_only), not hardcoded heuristics (BleachBit lesson).","Plan-then-execute: scan is a true dry-run of clean; same category/age filters; optional JSON plan export/import; never silent mass delete.","Risk is binding product policy: only risk=low may default_enabled and mutate; medium|high map to report_only behavior for mutation even if a pack file mis-sets flags.","Trash-first backends via trash crate (FreeDesktop/macOS/Windows); permanent is a second hard gate with typed DELETE phrase.","Contents-only delete_mode preferred so rule roots (Caches, Logs, Temp) are not removed.","Browser cache rules separate from cookies/logins/history; privacy high-risk stays off and never mixed into cache labels.","Dev reclaim prioritizes global download caches (npm/yarn/pip/Homebrew); project node_modules/target walks stay opt-in/report-only with markers and age.","Report-only for system landmines: Prefetch, Windows.old/WinSxS, Xcode Archives, Docker disks, pnpm store, old kernels, journal paths.","Never-touch and report_only cannot be overridden by user allowlists or rule YAML.","stdout holds table/JSON only; stderr holds progress/warnings so CI pipes stay clean; non-TTY never prompt-hangs.","Exit codes are API: 0 empty-or-full success, 1 partial/operational failure, 2 usage/config.","Honest sizing: measured reclaimable matched-file bytes only; no double-count, free-space-as-junk, or dirty-score meters.","macOS path priority: ~/Library/Caches+Logs, $TMPDIR, Homebrew, Quick Look; Xcode medium off; Mail/iOS backups report-only.","Linux: XDG cache/thumbnails/user temp/logs; package caches privileged/off; Flatpak/snap leftovers report-only.","Windows: user TEMP and thumbnail caches default; system TEMP/Prefetch/browser privacy opt-in or report-only.","Unbounded home/disk walks forbidden; every rule has explicit roots, max_depth, and optional min_age.","Native-tool preference layer records recommended commands for report-only hits without shelling out from rule YAML.","Post-MVP plugins (docker prune, leftovers, journal vacuum) must emit the same Candidate model and default to report-only.","MVP is path-based only; no code execution in rules."],"cli_commands":["ppduster doctor","ppduster rules list [--all] [--risk low|medium|high|report-only]","ppduster rules show <rule-id>","ppduster categories [--all]","ppduster scan [-c CATEGORY...] [--all] [--min-age N] [--limit N] [-o table|json]","ppduster scan --plan path/to/plan.json","ppduster clean [-c CATEGORY...] [--all] [--min-age N] [-o table|json]","ppduster clean -c caches,logs,temp --yes","ppduster clean --from-plan plan.json --yes","ppduster clean --yes --permanent","ppduster suggest","ppduster --rules-dir <path> <subcommand>","ppduster --version | --help"],"default_categories":["caches","logs","temp","thumbnails","package-cache","saved-state","leftovers","app-cache","macos-user-caches","macos-user-logs","macos-saved-state","macos-user-tmpdir","macos-homebrew-cache","macos-quicklook-cache","xdg-user-cache","linux-user-logs","windows-temp-user","windows-thumbnail-cache","js-package-caches","python-package-caches","app-spotify-discord-slack-zoom-caches","report-only:browser-cache","report-only:dev-artifacts","report-only:docker-disk","report-only:stores","report-only:system","report-only:privacy","report-only:leftovers","report-only:mail-downloads","report-only:ios-backups"],"file_layout":["Cargo.toml","src/main.rs","src/lib.rs","src/rules.rs","src/scan.rs","src/safety.rs","src/risk.rs","src/clean.rs","src/report.rs","src/plan.rs","src/audit.rs","src/suggest.rs","rules/macos.yaml","rules/linux.yaml","rules/windows.yaml","rules/dev.yaml","rules/apps.yaml","tests/safety_tests.rs","tests/rules_schema_tests.rs","docs/safety.md","docs/categories.md","research/PRODUCT_ANALYSIS.md","research/repos_collected.json","README.md","LICENSE"],"modules":["rules: load/merge YAML packs (schema_version, pack_version), path-template expand, platform filter, risk defaults","scan: bounded walk of rule roots with age/glob/max_depth; size + rule-id attribution; open-file skip","safety: hard never-touch denylist, canonicalize, symlink stay-under-root, TOCTOU revalidate, refuse failed normalize","risk_gate: default_enabled only for risk=low; medium|high|report_only never mutate on normal clean even if pack flags mis-set","clean: dry-run default; trash crate backend; permanent only with --permanent + typed DELETE; deepest-first; skip locked","report: human table + stable JSON; category totals; deleted/skipped/errors; stdout data, stderr progress on TTY","plan: export/import apply plan sharing scan/clean filters so dry-run is a true preview","audit: local JSONL deletion log (method, rule id, size, skips/errors); no path telemetry off-box","doctor: packs loaded, platform, denylist sample, trash backend health","cli: clap subcommands, shared filters, exit codes 0/1/2 (success, partial/ops failure, usage/config)","native_hints (suggest): non-executing OS/PM guidance strings for report-only hits"],"mvp_scope":"Ship a solid cross-platform CLI+library: load shipped packs (macos/linux/windows/dev/apps), scan and clean with identical filters, Trash-first deletes for default low-risk only after --yes, JSON/table reports, plan export/import, doctor/rules/categories/suggest, hard never-touch + risk_gate + safety tests, local deletion audit log. Default preset is low-risk user-space regenerable junk on the host platform (caches/logs/temp/thumbnails/package download caches/light leftovers/saved-state/known app caches). Medium+ and report_only categories (browser disk cache, Xcode DerivedData/DeviceSupport/Archives, Docker disks, node_modules/target, pnpm store, Prefetch, WinSxS/Windows.old, privacy artifacts, mail/iOS backups, system landmines) scan and report with native-tool hints but never delete on the normal clean path. Explicitly out of MVP: GUI/menu-bar, Smart Care upsell, registry cleaners, malware scanners, secure shred/free-space wipe, full AppCleaner leftover uninstall graph, duplicate/similar-media engines, Docker/volume/package/journal mutation backends, unbounded home walks, scheduled daemon, freemium/telemetry, history/restore UX, interactive TUI multi-select of medium risk. macOS is primary validation OS; Linux and Windows packs ship and work without elevation for user-scope targets. Plugins later (docker prune, journalctl, apt clean) must share Candidate model and default report-only.","product_name":"ppduster","repo_count_covered":100,"safety_model":"Fail-closed safety-maximalist model. (1) scan never mutates; clean without --yes is dry-run of the same Candidate pipeline/filters. (2) Default-enabled rules are risk=low only with min_age_days floors (typically 2-14d); optional global --min-age override. (3) Medium, high, and report_only are inventory-only on the normal clean path even with --yes; no MVP flag bulk-upgrades medium+ to delete. (4) OS Trash/Recycle is the only default mutation; --permanent requires typed confirm phrase DELETE, not --yes alone. (5) Hard never-touch denylist (Documents/Desktop/Downloads/Pictures, Mail/Messages/Keychains, .ssh/.gnupg, home roots, /System, /usr, WinSxS, Program Files) wins over rules and user allowlists. (6) Canonicalize paths; refuse failed normalize and symlink escapes past rule roots. (7) Prefer contents-only delete_mode so rule roots (Caches, Logs, Temp) are not removed. (8) Skip locked/in-use files; revalidate never-touch and existence at delete time. (9) Never-touch and report_only cannot be overridden by allowlists or rule YAML. (10) Honest measured reclaimable bytes only; full path+rule-id audit; partial cleans exit non-zero on failures. No root required for core MVP; no shred/wipe, registry, malware theater, telemetry, or scareware health scores.","stack":"Rust 2021 single binary + library (clap, serde_yaml/json, walkdir, globset, trash, dirs, tabled/bytesize, chrono, anyhow/thiserror). Declarative YAML rule packs as data under rules/ (macos, linux, windows, dev, apps). No GUI runtime, no network in core; MIT; offline local-first.","summary":"Local-first, open-source, rule-driven junk cleaner CLI (macOS first, then Linux, then Windows). Reclaims space from regenerable user caches, logs, temp files, package-manager download caches, thumbnails, saved application state, and selected app caches via versioned YAML rule packs. Core loop is scan (honest sizes, rule-id attribution) then clean dry-run then explicit --yes into OS Trash; medium+ and report_only stay inventory-only on the normal path. No scareware scores, telemetry, GUI, malware claims, registry cleaners, or silent bulk wipe."}
```

## Commercial (JSON)

```json
[{"features":["System junk: caches, logs, temporary and system-generated clutter (category-level rules)","Named junk markers include .DS_Store and .Spotlight-V100; trash emptying (incl. historical iPhoto Trash)","Language files and (classic) universal-binary architecture slices as space reclaim","App leftovers / broken preference-pane remnants after incomplete uninstalls","Mail attachment cleanup; large third-party caches called out (e.g. Spotify, Dropbox)","Large & Old Files / My Clutter: large files, forgotten downloads, duplicates, similar photos","Space Lens visual disk map; later Cloud Cleanup for iCloud Drive, Google Drive, OneDrive","Full app Uninstaller (app + support/install leftovers) and multi-app Updater (Sparkle + App Store)","Malware/adware scan via Moonlock Engine (not positioned as full antivirus)","Privacy cleanup: browser data, recent items, app permissions, untrusted Wi-Fi networks","Performance maintenance: DNS flush, Spotlight/Mail reindex, login items and Launch Agents/background control","Smart Scan / Smart Care one-pass multi-routine; menu-bar resource/threat monitor","Safety Database whitelist so bulk delete avoids known-critical files","CleanMyMac CLI (2026): clean junk, dev-tool caches (Homebrew/npm/Docker/etc.), purge project artifacts"],"lessons":["Copy category taxonomy and risk tiers (safe caches/logs vs high-risk binary/language stripping)","Copy Safety Database / hard deny-lists for critical system paths","Copy scan → dry-run report → explicit select → delete; never silent default delete","Copy leftover-aware uninstall model (bundle + known ~/Library and /Library support paths)","Copy composable CLI categories (analyze vs clean <category>) for scripting and libraries","Avoid scareware, RAM-boost theater, and marketing incomplete malware as protection","Avoid default telemetry of file paths, app inventories, and device identifiers; local-first open source","Avoid auto-remove-all for non-experts; keep high-risk categories opt-in with restore story","Avoid subscription walls on basic cache/log cleaning; keep helpers fully uninstallable"],"monetization_hooks":["7-day full-feature free trial then paywall","Annual and monthly subscriptions; higher one-time license","One-time licenses that may exclude free major-version upgrades","Basic vs Plus tiers gating Space Lens / Cloud Cleanup","Multi-device seats (1/2/5) and business/volume licensing experiments","Setapp bundle distribution","App Store edition with reduced features vs direct MacPaw download","Cross-sell (Setapp, CleanMyPhone, Moonlock branding, upgrade discounts)"],"product":"CleanMyMac X (MacPaw; later CleanMyMac v5 / CleanMyMac CLI)","risks":["Apple Support Communities often advise against it after breakage and leftover-helper reports","Malware module incomplete in third-party tests (misses/leaves remnants); not a full AV replacement","Bulk cache deletion can temporarily slow apps while caches rebuild","User reports of aggressive or incorrect moves/deletes when rules misfire","Privacy policy collects cleanup log paths/sizes, installed apps, device/disk info, Mac Health; malware automation may upload suspect binaries","Closed source prevents independent audit of deletion and network behavior","Freemium pressure: trial expiry, Basic/Plus gates, App Store missing features, paid major upgrades","Scare/upsell perception: urgency messaging and subscription pricing vs free macOS Storage tools"],"ui_model":"Primary GUI suite with sidebar categories (Cleanup, Protection, Performance, Applications, My Clutter/Space Lens) plus Smart Scan/Smart Care one-button multi-scan; results shown as summaries/tiles with Review (file list) or Remove; Safety Database + confirm-before-delete; menu-bar monitor. Not a pure wizard. Family also ships CleanMyMac CLI (category subcommands: clean, purge, analyze). No public open library API; closed-source commercial product."},{"features":["Health Check one-click clean: Recycle Bin, unused system/app temps, browser cookies/history/cache/trackers","Custom Clean with Windows + Applications category checkboxes, Analyze preview, then Run Cleaner","Browser junk/privacy: Edge, IE, Firefox, Chrome, Opera, Safari (history, cookies, cache, form autocomplete)","System junk: memory dumps, log files, file fragments, system caches, application temp data","Windows path patterns: %TEMP% (AppData\\Local\\Temp), C:\\Windows\\Temp, Prefetch, Temporary Internet Files, browser Code Cache","Safety options: skip temps in use; optional Recycle Bin min-age (~24h); some temp rules target files idle ~48h","Registry cleaner: missing shared DLL refs, unused file-extension registrations, missing application paths","Tools: uninstall programs, manage startup apps, delete System Restore points","Drive free-space / MFT wipe (Drive Wiper; reduced or absent in some CCleaner 7 builds per community)","Smart Cleaning (ex-Active Monitoring): background/scheduled or browser-close cleaning","Software Updater and Driver Updater (often Pro-tier)","Performance Optimizer and multi-user cleaning (Pro marketing: Cleans Everywhere)","CLI automation: documented switches including /AUTO clean-and-exit","Community Winapp2.ini declarative rules for extra app caches/logs/MRUs (off by default)","Optional CCleaner Browser (Chromium/Avast Secure Browser family) via installer","Mac/Android/iOS cleaners exist; product remains Windows-first historically"],"lessons":["Require scan/plan/preview before any delete; always support dry-run","Use declarative, inspectable path rules (Winapp2-style) instead of opaque heuristics","Default only high-confidence space reclaim (%TEMP%, Windows\\Temp, Recycle Bin); privacy wipes opt-in","Do not ship registry repair as a default or primary feature","Apply safety guards: skip in-use files, optional min-age, clear warnings for cookies/logins","No silent telemetry, forced startup, or bundled AV/browser","Harden distribution: signed reproducible builds; treat supply chain as a first-class risk","Avoid performance marketing for cache deletion; state space/privacy goals honestly","Expose first-class CLI/library API for automation without GUI nags","Keep aggressive community rulesets off-by-default and labeled by risk"],"monetization_hooks":["Free tier limited to basic Health Check / current-user clean","Pro subscription: Smart Cleaning schedules, multi-user Cleans Everywhere, Performance Optimizer","Pro: Driver Updater, Software Updater, auto history/junk guards","Professional Plus / Premium Bundle: Recuva, Speccy, Defraggler","Installer upsell of CCleaner Browser and Avast/Norton ecosystem products","Business/Cloud centrally managed edition; Free restricted for commercial use"],"product":"CCleaner (Piriform/Avast/Gen Digital)","risks":["2017 supply-chain attack: signed CCleaner 5.33 shipped Floxif backdoor to ~2.27M machines; ~40 got second-stage payload targeting tech firms","2019 second attempt to inject malware into CCleaner build pipeline (reported unsuccessful)","Post-Avast bundling of antivirus/other apps; Windows Defender flagged Free as PUA (2020) over unexpected bundled software","Telemetry/Active Monitoring controversy (v5.45): hard-to-disable monitoring and data collection backlash","Registry cleaners generally discouraged by Microsoft guidance; can break apps with limited performance benefit","Aggressive privacy cleans log users out and reset cookies/site state","Over-cleaning caches/Prefetch can worsen app launch times as data is rebuilt","Freemium nagware, Pro feature gates, and cross-sell (Browser, suite tools, Gen Digital security products)","CCleaner 7 reported to remove easy close / disable-run-on-startup options","Free historically limited vs Pro for multi-user profiles and automation completeness"],"ui_model":"GUI freemium: (1) Health Check guided one-shot, (2) Custom Clean category tree with Analyze then delete, (3) Smart Cleaning automation, (4) Tools (Registry/Startup/Uninstall/Drive Wiper/updaters). CLI /AUTO for unattended runs. v7 modernized UI but reduced quit/startup control."},{"features":["App/browser caches and system temporary files (Linux ~/.cache and /tmp; Windows %TMP% and profile temps)","Application and system logs (including Windows Defender logs in practice)","Browser privacy artifacts: cookies, URL/history, form history, sessions, crash reports, site data (LocalStorage/IndexedDB), site preferences","Cookie manager (6.0+): keep selected cookies while cleaning Chromium- and Firefox-based browsers","Password wipe for major browsers (warning + expert-mode protection)","SQLite vacuum for browser DBs (space + speed without deleting data)","Deep scan leftovers: Thumbs.db, .DS_Store, *.bak/~ backups, node_modules, venv, __pycache__, Office-style tmp, VIM .swp under home or /","System: clipboard, Recycle Bin/trash, update installers, Windows Explorer shellbags/recent documents/thumbnails","Linux package cleaners: APT clean/autoclean/autoremove and /var/lib/apt/lists; DNF/yum/pacman-style cleaners","Linux localizations: unused locales under /usr/share/locale, man pages, Flatpak runtime locales (keep languages via prefs)","Windows registry key/value deletion via CleanerML; optional winapp2.ini community cleaners (2400+)","Custom user-defined paths under System; allowlist (skip list) for paths","Secure delete: single-pass overwrite of cleaner targets; shred arbitrary files/folders; rename before delete","Wipe empty (free) space on selected drives; fstrim on Linux partitions when available (6.0)","Linux-only: clear memory and swap; delete broken desktop shortcuts","Chaff: generate decoy data to raise cost of forensic recovery","CleanerML extensibility with env/XDG vars; paths for Firefox/Chrome profiles including snap/Flatpak","6.0 browser coverage: Vivaldi/Zen; expanded Chromium/Firefox artifact cleaners (shader/code caches, DIPS, IndexedDB, etc.)","Optional periodic update check (no auto-download); offline use without registration","Not a malware scanner: no antivirus engine or infection removal"],"lessons":["Default to dry-run/preview with explicit path listing and sizes before any delete","Ship a safe default profile; gate passwords, free-space wipe, and deep scan behind expert mode","Use declarative path rules with XDG/AppData variables; support Flatpak/snap profile layouts","Split reclaim-space, privacy-erase, and secure-wipe into distinct modes with different defaults","Document honest secure-delete limits (1-pass HDD; weak on SSD; prefer encryption)","First-class CLI: list, dry-run, clean, except, presets for automation and library embedding","Refuse to clean if target process is running; support allowlists and custom path include/exclude","Never follow junctions/symlinks into unexpected trees without an explicit policy","Deep scan needs age/size/denylist filters and forced interactive review","Crash-safe wipe: recoverable temp naming and a documented recovery cleaner","No ads, telemetry, or bundled crapware: primary trust differentiator vs freemium cleaners","Do not claim malware removal without a real scanner; do not oversell unrecoverability beyond OS/storage reality"],"monetization_hooks":["BleachBit itself: none (GPLv3, no ads, no upsells, optional update check only)","Freemium cleaners typically upsell: expanded app cleaner packs","Real-time junk monitors and scheduled automatic cleanup","Registry cleaners / startup managers / driver updaters","Bundled antivirus or 'PC health' suites","Paid multi-pass shredding marketed as DoD/Gutmann (often oversold)","Tech-support / remote-help subscriptions","Cloud backup or premium support tiers"],"product":"BleachBit","risks":["No undo: deletions permanent; overwrite and wipe empty space make recovery unlikely (ZDNET: create restore point first)","Aggressive options: browser passwords, cookies (logouts), sessions, DNF autoremove package removal","Wipe empty space fills the disk for minutes/hours; interrupting leaves huge random files in ~/.cache, /tmp, or %TMP%","Deep scan can flag wanted backups, node_modules, or project files if preview is skipped","SSD free-space wipe is weak (wear leveling) and causes write wear; less useful with full-disk encryption","Admin/root vs user split: system cleaners need elevation; dual runs may be required on Linux","Cleaning while apps (browsers) are open can fail or corrupt profiles","Antivirus false positives on BleachBit installers/binaries due to shredding heuristics","Political brand association (Clinton email server used BleachBit) and early 'Linux registry cleaner' controversy","Historical Recycle Bin junction/symlink following could delete unintended data (called fixed as important in 6.0)","Official product has no ads/upsell/telemetry beyond optional update check; freemium competitors are the upsell contrast"],"ui_model":"GUI: left-side tree of cleaners with nested options, per-option descriptions/warnings, Preview (list every file/size) then Delete with confirmation; Expert mode (5.1+) locks high-risk options (passwords, empty-space wipe, deep scan, shellbags, etc.) until enabled; cookie manager for selective cookie keep; shred files/folders from menu; preferences for overwrite-on-clean, drives for free-space wipe, languages, allowlist, custom paths. CLI: bleachbit / bleachbit_console.exe with --list-cleaners, --preview, --clean, option wildcards (app.*), --preset, --all-but-warning, --except, --overwrite, --shred, --wipe-empty-space for scripts/cron/Task Scheduler. Extensible CleanerML and optional winapp2.ini. Not a one-click smart-scan wizard; user-driven checkbox selection."},{"features":["System Cleaner categories: package caches, crash reports, application logs, application caches, trash","Package cache scan/delete: /var/cache/apt/archives/ (APT) and /var/cache/pacman/pkg/ (pacman)","Crash reports under /var/crash (files)","Application logs: top-level files only in /var/log (not subdirectories)","Application caches: entries under ~/.cache (files and directories)","Empty Trash: ~/.local/share/Trash/files and ~/.local/share/Trash/info","Dashboard: CPU, memory, disk, network rates, basic system info","Startup Apps manager (autostart entries, often ~/.config/autostart)","Services: search, start/stop, enable/disable system services","Processes: list/sort by PID/CPU/memory, end process, refresh rate","Uninstaller for installed packages (dpkg/apt) and Snap packages (later versions)","Resources graphs (~60s) for CPU cores, RAM, disk, load average, network; disk pie chart in later UI","APT Repository Manager: enable/disable/edit/delete package sources","File Search (beta): files, directories, symlinks with advanced options","Helpers: hosts file manager; limited GNOME Settings tweaks; resource threshold alerts"],"lessons":["Copy scan-then-select with sizes and default-unchecked results rather than silent one-click mass delete","Copy multi-package-manager awareness for real cache roots (apt archives, pacman pkg cache, etc.)","Copy open-source no-adware model; optional donations instead of nags or bundled upsells","Avoid treating all of ~/.cache as equally safe; split high-risk trees (browsers) with strong warnings","Default-protect critical logs (apt/dpkg/Xorg-class) instead of offering them as ordinary junk","Prefer library+CLI with dry-run, explicit path rules, and clear failure when privileges are missing","Prefer quarantine/trash or --force over immediate irreversible rm -rf as the only path","Stay maintained; an abandoned system cleaner becomes a liability in distro repos","Do not bundle process kill, service disable, hosts edits, or GNOME tweaks into a junk cleaner without hard gates","Require confirmation and safety lists for package removal; never market core packages as casual cleanup"],"monetization_hooks":["Stacer itself: free MIT with Patreon/OpenCollective donations only (no freemium paywall found)","Class comparison (not Stacer): freemium cleaners often upsell scheduled/auto clean, premium monitors, driver updaters, malware add-ons, and multi-device licenses"],"product":"Stacer","risks":["Upstream abandoned: GitHub warns no further releases, so bugs and breakage accumulate","App-cache deletes from ~/.cache can slow app launches and disrupt sessions (e.g. browser re-login)","Deleting application logs can remove useful or important files; docs warn to keep current X.org, Apt, and Dpkg logs","Cleaner uses aggressive sudo rm -rf on selected paths with no documented dry-run, quarantine, or undo","Uninstaller can list and remove core packages; reviews note weak confirmation and no undo","Service toggles can break the session or require a reboot if critical services are stopped","Historical privilege UX: wrong sudo password could still open the UI while actions fail silently","Coarse categories (especially whole ~/.cache and top-level /var/log files) invite over-deletion via select-all","Not a malware or privacy product; optimizer framing can overstate safety/security value","Reports of hangs/slow scans on large cache trees on some distros"],"ui_model":"Qt desktop GUI with left-sidebar sections (all-in-one optimizer, not a CLI). System Cleaner flow: enable category checkboxes (or select all) -> System Scan -> tree of results with name and size (items unchecked by default) -> check files/categories -> Clean. Privileged actions use sudo; selected non-trash paths are removed with sudo rm -rf; trash is wiped recursively under the user Trash directory. Also includes monitoring, process/service control, uninstaller, APT sources, search, hosts, and GNOME tweaks."},{"features":["User/system/app/font/internet cache cleaning (OnyX: System/User/Internet/Font caches; CleanMyMac System Junk)","Log and diagnostic report cleanup (OnyX Cleaning: Log messages/reports; CleanMyMac junk logs)","App leftover uninstall: ~/Library/{Application Support,Preferences,Caches,Logs,Containers,Saved Application State} plus LaunchAgents/Daemons","SmartDelete-style trash watch for .app removals with selectable related files (AppCleaner)","Force Empty Trash and rebuild indexes/DBs (OnyX: Trash, Launch Services, Spotlight, Mail, XPC/CoreDuet)","Mail attachment local copies with review before delete (CleanMyMac Mail Attachments)","Large/old/user bulk: ~/Downloads, ~/Movies, ~/Music, iOS backups ~/Library/Application Support/MobileSync/Backup","Visual or size-ranked disk map including hidden/System Data, purgeable space, APFS snapshots (DaisyDisk)","Admin/Full Disk Access scan for restricted folders (DaisyDisk, OnyX, CleanMyMac deep modules)","Optional privacy wipe: browsing history/recent items (CleanMyMac Protection); not core junk reclaim","Optional malware module only with real engine (CleanMyMac Moonlock); high false-positive risk if naive","Dry-run scan with per-path size, category, confidence, and user confirm before delete (DaisyDisk/AppCleaner pattern)"],"lessons":["Default to dry-run; show exact paths and sizes; never silent bulk delete","Hard-block system-critical paths (DaisyDisk safety stoppers)","Prefer reclaiming large user bulk (downloads, backups, sims, leftovers) over RAM/optimize myths","Narrow high-trust modules (uninstall leftovers, caches, logs) beat all-in-one scareware","OS-version-aware behavior and explicit categories reduce breakage (OnyX)","No default telemetry; scan metadata only when possible (DaisyDisk privacy model)","Avoid malware theater without a low-FP engine; never sell routine state as infection","Keep apply step human-confirmed; confidence scores for fuzzy leftover matching","Treat auto-update signing and privilege as security-critical (CCleaner lesson)","Document what is safe vs use Apple app UIs for Photos/Time Machine content"],"monetization_hooks":["Free scan / trial that unlocks paid Clean (CleanMyMac-style freemium)","Subscription vs one-time license and multi-Mac seats","Malware/real-time protection upsell (Moonlock tier)","Cloud storage cleanup to avoid paid iCloud/Drive upgrades","Menu bar monitoring stickiness (CPU/RAM/storage)","Lifetime/corporate licenses (DaisyDisk model for analyzers)","Donationware without feature hostage (OnyX)"],"product":"ppduster design brief (OnyX/AppCleaner/DaisyDisk/CleanMyMac)","risks":["False positives labeling legitimate system/Gatekeeper data as malware (reported with OnyX-style scans)","Name-matched leftover deletion removing shared or still-needed Library files (AppCleaner-class tools)","Aggressive cache/log wipes that refill quickly or break apps until rebuild","Deleting under ~/Library/Application Support without understanding app libraries (DaisyDisk warns)","Breaking media libraries if content deleted outside Photos/Music/Time Machine UIs","Manufactured urgency and free-tier nags selling paid clean (CleanMyMac/ freemium peer complaints)","Default telemetry and privacy concerns in commercial cleaners","Supply-chain malware risk for privileged auto-updating cleaners (CCleaner 2017, Windows precedent)","Wrong OS-version or overly deep system tweaks on modern SIP-locked macOS (OnyX version pinning)","Community skepticism that macOS self-maintenance makes bulk cleaners low-value or harmful"],"ui_model":"Preferred: CLI+library with scan→plan(dry-run)→apply; category checklists (OnyX-style); leftover file picklists (AppCleaner); optional size-map exploration (DaisyDisk). Avoid freemium one-click Smart Care that frames routine state as problems (CleanMyMac)."},{"features":["Temp and log cleanup: Windows %TEMP%/%WINDIR%\\Temp, app logs, memory dumps (*.dmp), thumbnail/icon caches","Browser junk: cache, history, cookies, autocomplete for Chrome/Firefox/Edge/IE/Opera/Safari (20+ browsers in Wise Store listing)","Windows Update / installer residues: downloaded update files, installer baseline cache, obsolete installation files","App leftovers: Office, Adobe, Java, Flash, WinRAR/WinZip, Steam HTML cache, 300+ apps (Wise); CCleaner app-specific cleaners","Recycle Bin / Recent Documents / Quick Access style traces (Wise, PortableApps, Glary Tracks Eraser)","Privacy tracks: visited URLs, typed URLs, recent docs, index.dat, form history (Glary Tracks Eraser; Wise warns on passwords/cookies)","Advanced extension scan: *.tmp *.temp *.chk *.gid *.dmp plus custom types and empty custom folders (Wise Advanced Cleaner)","System slimming: IME packs, help files, wallpapers, sample media (Wise; can break SFC completeness)","Registry invalid-entry cleaner (CCleaner, Glary; Microsoft does not support this class of tool)","Secure delete / overwrite and free-space or empty-space wipe (Wise Safe Deletion, CCleaner MFT wipe, BleachBit shred/wipe-empty-space)","HDD defrag + SSD TRIM (Wise Disk Cleaner)","Duplicates, large/old files, disk space analyzer, startup manager, software updater (Glary suite; CleanMyMac clutter tools)","Mac system junk, complete app uninstall leftovers, malware scan, cloud cleanup, Smart Care multi-routine (CleanMyMac)","macOS cache targets commonly cited by cleanup guides: ~/Library/Caches, logs, Trash, System Data (snapshots/backups/app caches)","Extensible rules: BleachBit CleanerML + winapp2.ini; SQLite vacuum-style specialized actions","Scheduled/auto clean: Wise AutoClean; Glary Pro scheduled maintenance; BleachBit cron/Task Scheduler CLI"],"lessons":["Default to dry-run/preview and Recommended soft targets only; require explicit opt-in for privacy and OS-slimming categories","Ship first-class CLI + library API like BleachBit (list, preview, clean, except, warning flags) for automation without a GUI","Use declarative community-extensible cleaner rules (CleanerML/winapp2-style) instead of opaque binary scanners","Per-path exclude lists, open-location, and exportable scan reports build trust and recoverability","Do not make registry cleaning a core feature; treat Microsoft’s non-support as a product constraint","Never use fake threat counts, inflated freeable GB, or forced AV/browser bundles","Mark irreversible categories (passwords, cookies, restore points, system components) with hard warnings","Prefer offline, no-telemetry operation; any metrics must be opt-in and documented","Separate junk cleaning from malware claims unless real detection is in scope and independently validated","Document OS paths by platform (Windows Temp/Update/browser profiles; macOS ~/Library/Caches and System Data caveats; Linux ~/.cache and package caches)","If automation ships, schedule only the same safe preset as the default GUI—never silent deep wipe"],"monetization_hooks":["Free basic clean → paid suite (Wise Care 365, Glary Pro all-in-one)","Gate scheduler, deep clean, and auto privacy-on-shutdown behind Pro (Glary, CCleaner)","Subscription Mac maintenance (CleanMyMac trial → monthly/yearly)","Cross-sell antivirus, VPN, Chromium browser, driver updater (CCleaner/Gen Digital ecosystem)","Upsell file recovery, shredder, duplicate finder, priority support","Restrict Free to home use; charge commercial/network licenses (CCleaner)","Scareware/PUP affiliate installs (anti-pattern: MacKeeper-class ads—avoid entirely)"],"product":"Junk cleaners (Wise Disk Cleaner, Glary Utilities, CCleaner, BleachBit, CleanMyMac)","risks":["Registry cleaners: Microsoft unsupported; can delete needed keys and break apps/OS with little speed benefit","False confidence from huge freeable-space claims and scareware-style problem counts (CCleaner nags; MacKeeper PUP/affiliate fake scans)","Aggressive or irreversible deletes: passwords, cookies, System Restore points, OS help/IME/slimming (Wise SFC warning)","CCleaner 2017 supply-chain malware in official build (~2.27M machines); later breach attempt; trust damage for all cleaners","Telemetry and hard-to-disable monitoring (CCleaner Active Monitoring/Smart Cleaning controversy)","Bundled AV/browser/PUA installers (CCleaner/Avast; Microsoft PUA flag for bundling)","Unwanted persistence: tray apps, scheduled tasks, freemium nags, hard-to-exit startup (CCleaner v7 complaints)","System Slimming / deep OS cleanup that later fails integrity checks or removes needed input methods","Secure wipe misused as performance tool: slow, does not free extra space when wiping empty space","Suite bloat increases attack surface and confuses safe junk vs security claims"],"ui_model":"Dominant pattern is scan-then-select-then-delete: category trees with Recommended presets (Wise Common Cleaner; CCleaner Analyze/Clean; BleachBit option checkboxes + preview). Suites add a One-Click/Smart/Health wizard (Glary 1-Click Maintenance, CleanMyMac Smart Care, CCleaner Health Check) plus deep modules. Power path is CLI: BleachBit --list-cleaners/--preview/--clean/--preset/--all-but-warning/--except for scripts and scheduled tasks. Safety UX includes ignore/exclusion lists, open file location, export results, and explicit warnings on passwords/cookies (Wise, BleachBit)."},{"features":["Windows Storage Sense: auto-delete temp files on low disk or schedule (system drive only)","Windows: Recycle Bin age policy and optional Downloads not-opened-for-N-days policy","Windows: OneDrive/cloud dehydrate to online-only for inactive files (not permanent delete; Always keep exempt)","Windows Temporary files UI: Windows.old/previous installs, Update leftovers, Delivery Optimization, thumbnails, browser temp","macOS Storage dashboard: category sizes (Apps, Documents, Messages, Photos, Mail, TV, Trash, System Data/Other)","macOS Store in iCloud: Desktop/Documents, Photos, Messages with local recent/optimized copies","macOS Optimize Storage: remove watched Apple TV content; keep only recent Mail attachments when space needed","macOS Empty Trash automatically after 30 days","macOS automatic safe cleanup of caches/logs: temp DB files, interrupted downloads, staged updates, Safari website data","User-directed free space: Downloads, unused apps, old iOS backups, Mail junk/deleted, streamable media, unused user accounts","Safe Mode temporary system cache clear for install headroom","Common junk path categories third parties also target: %TEMP%, C:\\Windows\\Temp, ~/Library/Caches, ~/Library/Logs, Trash","App leftover locations (uninstall tools): ~/Library/Application Support, Preferences, Containers, Saved Application State, LaunchAgents","BleachBit-class extras (not built-in): multi-app browser cleaners, secure delete, free-space wipe, CLI, CleanerML rules","CCleaner-class extras (not built-in): registry cleaner, startup manager, System Restore point wipe, driver/software updater (paid)"],"lessons":["Default only OS-safe temp/trash/cache categories; require opt-in for Downloads, Documents, browser profiles, Application Support","Always scan and list absolute paths with sizes and risk tier before delete; dry-run by default for CLI/library","Use age/last-access policies like Storage Sense instead of blanket folder wipes","Separate dehydrate/online-only from permanent delete in wording and APIs","Do not implement registry cleaning or fake RAM/CPU boost claims","No telemetry-by-default, no silent bundled extras, no scare-ware gauges for upsell","For app leftovers, bind to uninstalled app identity and preview like AppCleaner; never silent deep Library clean","Prefer open, auditable cleaner rules (CleanerML-style) over proprietary black-box junk databases","Scope clearly: which volumes, which users, what is purgeable vs user data","Malware belongs to real security tools, not as a junk-cleaner checkbox that invents confidence"],"monetization_hooks":["Scheduled/auto Smart Cleaning (CCleaner Pro, CleanMyMac)","Driver updater and software updater upsells","Malware/real-time protection modules","Multi-device, business, and network editions","Subscription vs forced upgrade after rebrand/version drop","iCloud/cloud storage plan upgrades when optimize storage is used (Apple)","Bundle into suite (Setapp, Gen Digital/Avast)","Secure wipe / deep privacy clean gated behind paid tiers"],"product":"Built-in OS cleaners (Windows Storage Sense + macOS Storage/Optimize Storage)","risks":["Downloads or user-content cleanup deletes real files if age policies are enabled carelessly","Registry cleaners: Microsoft and experts advise against; risk of breakage with no performance benefit","False positives: cleaners mislabel user data as cache (e.g. music libraries, needed app support files)","Aggressive app leftover deletion can remove shared prefs, licenses, or data still needed after reinstall","CCleaner 2017 supply-chain malware in official build; later telemetry and bundling/PUA controversies","Freemium scare metrics, nag screens, and upsell of driver updater/AV/browser bundles","Opaque System Data/Other on macOS drives users to over-aggressive third-party wipes","Privacy clean of browser profiles logs users out and drops sessions/passwords/cookies if selected","Free-space wipe/shred is slow and can wear SSDs; not equivalent to secure erase of whole disk","Cloud optimize frees local space but depends on iCloud/OneDrive quotas and network for rehydrate"],"ui_model":"Policy-driven automation (Storage Sense schedule/low-disk; macOS Optimize/iCloud recommendations) plus category storage dashboard with opt-in Temporary files / More Info lists; explicit user confirmation for Downloads and content deletion; third-party pattern is checkbox categories or one-click Smart Care then review-delete (prefer dry-run report over opaque clean-all)"},{"features":["Scan rebuildable workspace dirs: node_modules, Pods, .build, .gradle, build, target, __pycache__, .next/.vite/.turbo (profile-based)","Package-manager caches: ~/.npm/_cacache, Yarn cache (~/Library/Caches/Yarn), pnpm store, ~/.bun/install/cache","JVM/language caches: ~/.gradle/caches+daemon, ~/.m2/repository, pip/cargo/go-build/pub caches","Xcode/macOS dev: ~/Library/Developer/Xcode/DerivedData, Archives, *DeviceSupport, Caches/com.apple.dt.Xcode, CoreSimulator","Docker reclaimables via system df + prune: stopped containers, unused networks, dangling/unused images, build cache; volumes as separate high-risk tier","Size + last-modified/age ranking and sort; dry-run; JSON/stream + library API for automation","Risk tiers: safe cache vs costly reinstall vs data (named volumes, archives, device support symbols)","Sensitive/generic-name warnings (app-bundled node_modules; basename traps like Library/out/deps)","Prefer native tool prune (docker/npm/pnpm/yarn) when safer than raw recursive delete"],"lessons":["Rebuildable-only catalogs with ecosystem profiles beat vague 'junk' claims","Never auto-select items; interactive consent is the DevCleaner/npkill trust pattern","Separate inventory (df/report) from mutate (prune/delete) like Docker","Warn and gate high-impact classes: volumes, archives, device support, global package stores","Expose dry-run, JSON, and a library API for CI and custom UIs","Show size and staleness so users delete old projects first","Limit default scope to user Home/workspaces; opt-in broader roots","Use path-context rules, not basename-only matching","Stay open, tip/donation optional, no malware/optimizer paywalls or opaque telemetry"],"monetization_hooks":["Optional tips/donations while core CLI stays free (DevCleaner/npkill pattern)","Paid GUI, scheduled reminders, or multi-machine reports without locking scan/delete","Avoid freemium 'scan free / clean paid' and malware-upsell suites used by general cleaners"],"product":"ppduster","risks":["Docker --volumes / volume prune can destroy DB and app state when containers are stopped","docker system prune -a removes unused tagged images, forcing re-pulls","Desktop Docker prune may not free host disk until VHDX/Docker.raw compact or Clean/Purge data","Deleting Archives or Device Support loses release history and forces large redownloads","Basename-only targets can hit wrong trees (Unity Library vs ~/Library; generic out/deps)","delete-all / clean-all without dry-run breaks system apps that ship node_modules","Size mis-estimates and non-undoable deletes; cleaning while Xcode/Docker busy causes odd failures","Industry freemium cleaners associated with upsell, telemetry, and past malware scandals—avoid that trust model"],"ui_model":"CLI+library defaulting to scan-only report, then interactive TUI list (npkill-style multi-select by path/size/age) and/or category panels (DevCleaner-style); confirm per risk tier; optional --force after dry-run; no wizard and no pre-selected deletes"}]
```

## Repos (JSON)

```json
[{"language":"Python","name":"BleachBit","safety":"Mandatory Preview before Delete; per-cleaner option toggles; keep/whitelist lists; warnings on destructive options; optional shred; CLI and GUI","stars_approx":"6.5k","steal":"Ship junk definitions as external CleanerML-style rule packs so the engine stays generic and cleaners are community-editable data","technique":"Declarative CleanerML XML rule DB (globs, walk.all, SQLite-aware actions, OS path vars, running-process guards); not a blind full-disk heuristic scan","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Application/browser caches, cookies, history, temp files, logs, package leftovers; optional secure shred and free-space wipe for privacy and disk space"},{"language":"C","name":"rmlint","safety":"Never modifies FS by default; writes reviewable rmlint.sh/JSON/Python; shell script confirms before delete; can hardlink/symlink/reflink/trash instead of unlink","stars_approx":"2.4k","steal":"Plan-then-execute: emit an auditable delete plan (script/JSON) the user reviews and runs, never silent deletion as the default","technique":"Fast filesystem walk with size-group then multi-stage hashing; optional mtime/stamp incremental scans; Btrfs clone/reflink support; many output formatters","url_or_slug":"sahib/rmlint","what_it_cleans":"Duplicate files and directories, empty files/dirs, broken symlinks, non-stripped binaries, files with bad UID/GID"},{"language":"Python","name":"FSlint","safety":"Find-first workflow; user selects which groups or items to remove in the GUI; finders report until explicit delete/merge actions","stars_approx":"372","steal":"One small finder per junk class composed by a thin UI, so each heuristic is testable and optional","technique":"Directory walk plus modular shell finders (findup, finded, findid, findns, etc.) orchestrated by a GTK GUI or CLI","url_or_slug":"pixelb/fslint","what_it_cleans":"Filesystem lint: duplicates, empty directories, bad names, bad symlinks, temp files, dead user IDs, non-stripped binaries"},{"language":"C","name":"tmpwatch","safety":"--test dry-run; path/user/pattern excludes; skips symlink follow and race-prone cases; limited file types by default; cron-oriented","stars_approx":"5","steal":"Age-gated temp reaping with dry-run, excludes, and skip-if-open (fuser) as the default policy for volatile directories","technique":"Recursive walk with atime/mtime/ctime age threshold; exclude path/pattern/user; optional fuser open-file check; no symlink-follow into targets; no cross-filesystem jumps","url_or_slug":"pete4abw/tmpwatch","what_it_cleans":"Aged files under temp-like directories (e.g. /tmp); empty dirs; optional shred. Closest OSS core to classic Red Hat tmpwatch / Debian tmpreaper lineage"},{"language":"C++","name":"Stacer","safety":"User enables categories and scans before cleaning; no silent full-disk mode; coarser than BleachBit file-level preview (app cache/log caution advised)","stars_approx":"9.3k","steal":"Category dashboard with reclaimable size totals (package vs app cache vs trash) for a simple first-run cleaner UX over a safer detailed backend","technique":"GUI category scan over known cache/log/package/trash path sets; select categories, scan for sizes, then clean","url_or_slug":"oguzhaninan/Stacer","what_it_cleans":"Package caches, crash reports, application logs/caches, and trash (system cleaner); broader optimizer for startup apps, processes, services (project abandoned)"},{"language":"Rust","name":"Czkawka","safety":"GUI select-then-act (no auto wipe); optional move-to-trash (off by default so permanent delete is possible); CLI delete-method default NONE, dry-run for duplicates, hard-link option; confirmation dialog setting","stars_approx":"32500","steal":"Shared czkawka_core library plus on-disk hash cache so second scans of the same trees are dramatically faster while frontends (CLI/GUI) stay thin","technique":"Recursive walk with multi-threaded grouping: size prefilter then content hashes (e.g. blake3/xxh3); persistent hash cache for rescans; separate engines for perceptual image/video similarity and music tags/fingerprints","url_or_slug":"qarmin/czkawka","what_it_cleans":"Exact duplicate files (name/size/hash), empty folders and files, temporary files, big files, similar images/videos, same-music (tags or content), invalid symlinks, broken files, bad extensions, plus related media cleanup tools"},{"language":"Python","name":"dupeGuru","safety":"Reference-directory model and result grouping designed so originals are protected; user selects actions (delete/move/copy); filters and sort to weed out low-threshold false positives before acting","stars_approx":"7700","steal":"Reference folders: mark paths whose files are never deleted so the engine always treats them as keepers when matching against other trees","technique":"Directory scan with configurable engines: full content comparison, fuzzy filename matching, music-tag comparison, and fuzzy picture matching with tunable match thresholds","url_or_slug":"arsenetar/dupeguru","what_it_cleans":"Duplicate files by content or fuzzy filename match; specialized music-mode (tags) and picture-mode (similar images); move/copy/delete of selected dupes"},{"language":"C","name":"fdupes","safety":"Default is list-only; -d interactive preserve/delete prompts; -N noprompt keeps first file; remove-if-not-changed guards; deletion logging; warnings about symlink and double-path pitfalls","stars_approx":"3000","steal":"Interactive per-group preserve prompts (pick which file numbers to keep) plus optional deferred byte confirmation until just before deletion","technique":"Walk dirs; size and optional min/max filters; signature/hash comparison with optional signature cache database; byte-for-byte confirmation of matches; hardlinks normally treated as non-dupes unless overridden","url_or_slug":"adrianlopezroche/fdupes","what_it_cleans":"Content-identical duplicate files under given directories; interactive or batch deletion of extras in each match set"},{"language":"C","name":"jdupes","safety":"Default report/summarize actions; interactive delete path; optional no-prompt modes; build-time NO_DELETE can strip unlink code; hashdb written with restricted permissions to limit metadata leaks","stars_approx":"100 (Codeberg; GitHub upstream removed)","steal":"Persistent hash database so re-scanning multi-million-file cold archives finishes in seconds after the first full pass","technique":"fdupes-style walk with performance-focused partial/progressive hashing and match confirmation; optional persistent hash database for repeated scans of huge mostly-static datasets; hardlink-aware hash reuse","url_or_slug":"codeberg.org/jbruchon/jdupes","what_it_cleans":"Duplicate files; can delete, hardlink, symlink, or block-level dedupe; advanced filtering for large multi-platform trees (enhanced fdupes fork)"},{"language":"C++","name":"rdfind","safety":"Default only writes results.txt (report-only dry run); deletions/links require explicit flags (-deleteduplicates / -makehardlinks / -makesymlinks); UndoableUnlink-style careful unlink path; documents hardlink-group caveats","stars_approx":"1300","steal":"Cheap staged elimination (size -> first bytes -> last bytes -> checksum) plus argument-order priority so the user controls which tree wins as the original","technique":"O(N log N) pipeline: list files, optional collapse identical device+inode hardlinks, drop unique sizes, sample first bytes then last bytes, then full checksum (md5/xxhash); sort by device/inode for sequential reads; original chosen by CLI arg priority and depth","url_or_slug":"pauldreik/rdfind","what_it_cleans":"Content-identical redundant files for cleanup or backup compression; optional replace with hardlinks/symlinks or delete extras"},{"language":"Zig (2.x) / C (1.x LTS)","name":"ncdu","safety":"confirm-delete on by default; --disable-delete and -r read-only; delete/shell/refresh disabled on imported exports; --delete-command can use trash (e.g. gio trash) or interactive rm","stars_approx":"n/a (Forgejo; widely packaged)","steal":"Pluggable --delete-command (trash vs unlink) plus export/import so scan and destructive cleanup can be separated (remote scan, local careful delete)","technique":"Recursive filesystem walk with bottom-up size aggregation; exclude patterns/exclude-from; CACHEDIR.TAG support; optional one-filesystem and parallel scan; JSON/binary export for offline or remote-scan browse","url_or_slug":"code.blicky.net/yorhel/ncdu","what_it_cleans":"User-selected large files and directories found via interactive ncurses size browser"},{"language":"Rust","name":"dua-cli","safety":"Multi-stage interactive delete flow to prevent accidents; ignore patterns; non-interactive paths are analyze-only","stars_approx":"~6.1k","steal":"Multi-stage TUI delete confirmations combined with ignore files that never enter excluded trees (node_modules, targets, etc.)","technique":"Parallel directory walk and size graph; aggregate CLI plus interactive TUI; gitignore-style --ignore-from / DUA_IGNORE_FROM skips matches without descending","url_or_slug":"Byron/dua-cli","what_it_cleans":"User-marked unwanted files/directories in interactive mode (bulk delete optimized vs plain rm)"},{"language":"Rust","name":"dust","safety":"No delete/trash at all; pure reporting tool","stars_approx":"~12.1k","steal":"Largest-N hierarchy bar view that reveals buried large files even when parent dirs dominate (complements dir-first TUIs)","technique":"Parallel walk with smart prune to terminal-height largest entries; hierarchy-shaded bars; hardlink single-count; min-size, regex include/exclude, filetype grouping, collapse, JSON export","url_or_slug":"bootandy/dust","what_it_cleans":"Nothing in-process (read-only du alternative); surfaces largest paths so the user can clean externally"},{"language":"Rust","name":"diskonaut","safety":"Delete requires y/n confirmation; quit confirmation; direct unlink (no trash layer)","stars_approx":"~3.1k","steal":"Live treemap fill during scan plus a session space-freed counter that measures cleanup progress","technique":"Background scan indexes metadata to memory; progressive live treemap while scanning; drill-down navigation; tracks bytes freed after deletes","url_or_slug":"imsnif/diskonaut","what_it_cleans":"User-selected files and folders chosen from a terminal treemap navigator"},{"language":"Go","name":"godu","safety":"Never unlinks itself; space-mark then exit prints NUL-separated paths (-print0); discovery and destroy are separate processes","stars_approx":"~580","steal":"Mark-then-print0 pipeline so cleanup is composable with external trash/dry-run/review tools","technique":"Fast walk with min-size filter (default 10MB); carousel TUI mark/unmark; ~/.goduignore skips whole folder names (e.g. node_modules)","url_or_slug":"viktomas/godu","what_it_cleans":"Marked large files/folders only when the user pipes stdout to rm, mv, or trash"},{"language":"Python","name":"trash-cli","safety":"Moves to trash instead of unlink; interactive trash-restore with index/range selection and optional --overwrite; trash-empty <days> for age-based purge; README strongly discourages aliasing rm to trash-put due to different semantics.","stars_approx":"4450","steal":"Age-gated trash purge (trash-empty N) as a built-in retention policy for recoverable deletes.","technique":"FreeDesktop Trash spec: move targets into trash dirs (e.g. ~/.local/share/Trash/files) with .trashinfo metadata (original path, deletion date, permissions); trash-empty can filter by age in days; trash-rm matches patterns against trashed names.","url_or_slug":"andreafrancia/trash-cli","what_it_cleans":"User-specified files/dirs via soft-delete into the FreeDesktop.org trashcan (shared with KDE/GNOME/XFCE); also lists, restores, pattern-removes, and empties trash contents."},{"language":"Rust","name":"trashy","safety":"Always trashes on put (no permanent delete); selective restore/empty with filters; --force for scripted restore/empty; no macOS support; docs warn against aliasing rm.","stars_approx":"530","steal":"fzf multi-select restore/empty pipeline over trashy list for interactive, filterable recovery.","technique":"Fast put/list/restore/empty CLI over OS trash backends; restore/empty args default to regex (override with --match=exact); recursive put by default; optional fzf pipelines for multi-select.","url_or_slug":"oberblastmeister/trashy","what_it_cleans":"User paths moved into the system trash (Linux FreeDesktop + Windows recycle); manages list/restore/empty of trashed items. Not a junk scanner."},{"language":"Rust","name":"rip (Rm ImProved)","safety":"No data overwrite; --inspect prints size/preview then prompts; --unbury undoes last or named items (aims to be idempotent); --decompose permanently unlinks entire graveyard; deliberately not XDG-trash.","stars_approx":"1730","steal":"Path-mirrored graveyard layout so undo restores by original absolute path without a heavy index UI.","technique":"Rename/move under configurable graveyard (/tmp/graveyard-$USER, $XDG_DATA_HOME/graveyard, or $GRAVEYARD) mirroring absolute paths; numbered backups on path collision; .record deletion log; --seance lists deletions under cwd.","url_or_slug":"nivekuil/rip","what_it_cleans":"User-specified files/dirs by moving them into a private graveyard (not the desktop trash); undo via unbury; optional full graveyard wipe."},{"language":"Go","name":"gomi","safety":"forbidden_paths blocklist (/, /etc, trash backends, etc.); restore confirm optional; permanent delete from trash is opt-in and double-confirmed; prune requires double confirmation.","stars_approx":"560","steal":"Configurable forbidden_paths so delete/empty operations refuse critical system paths and the trash store itself.","technique":"Configurable strategy (auto/xdg/legacy ~/.gomi), multi-volume trash, concurrent ops; history filters (within_days, globs, size); --prune by duration ranges or orphans; TUI browse with search/preview.","url_or_slug":"babarot/gomi","what_it_cleans":"User-specified files as an rm-compatible safer delete into XDG or legacy trash; TUI restore; prune of old/orphan trash entries."},{"language":"Swift","name":"macos-trash","safety":"Soft-delete into system Trash only; --interactive/-i confirmation; when aliased as rm, extra rm flags are ignored rather than enabling hard delete.","stars_approx":"450","steal":"Use platform trash APIs that preserve Finder Put Back metadata instead of raw filesystem moves into ~/.Trash.","technique":"Native macOS trash APIs via a small Swift CLI (platform FileManager/trash integration rather than FreeDesktop or ad-hoc ~/.Trash moves).","url_or_slug":"sindresorhus/macos-trash","what_it_cleans":"User-specified files/folders by moving them into the macOS Trash (Finder-compatible). Note: macOS 15+ has a built-in trash; this OSS binary adds --interactive and fixes Put Back."},{"language":"C++","name":"Stacer","safety":"Category checkboxes then scan builds a tree of selectable files with sizes; per-item and select-all checkboxes before clean; package-cache/logs/cache deleted via sudo rm -rf on checked paths; trash emptied by removeRecursively on Trash/files and Trash/info; no dry-run mode; project marked abandoned","stars_approx":"9.3k","steal":"Two-step scan UI: categories first, then a sortable tree of concrete paths with sizes and independent checkboxes before any destructive sudo delete","technique":"Fixed path walks and package-manager detection: list files in known dirs (e.g. /var/cache/apt/archives, /var/cache/pacman/pkg, /var/crash, /var/log, ~/.cache); detect PM via executable presence (apt-get/dnf/yum/pacman/zypper) then shell out to dpkg/rpm/pacman/snap for package lists","url_or_slug":"oguzhaninan/Stacer","what_it_cleans":"Package caches (apt/pacman/etc.), crash reports under /var/crash, /var/log app logs, ~/.cache app caches, and user trash (~/.local/share/Trash); also uninstaller/services/startup tools beyond pure junk cleaning"},{"language":"Python","name":"Ubuntu Cleaner","safety":"Scan-before-clean with per-plugin and per-cruft checkboxes; UI lists names and sizes; apt/package work via AptWorker transactions rather than blind rm for kernels/autoremove; apt archive files deleted via privileged proxy; no trash-first path (direct os.remove/shutil.rmtree for cache plugins)","stars_approx":"256","steal":"Category-tagged plugin modules with a shared scan/clean signal protocol so new cleaners are drop-in files without core UI changes","technique":"Plugin janitor architecture: each cleaner is a JanitorPlugin loaded by category (system/personal/application); cache plugins use root_path+glob or os.walk/du; package plugins query AptWorker/python-apt cache and regex-match linux-image/headers kernels older than uname; privileged apt cache deletes go through a DBus daemon proxy","url_or_slug":"gerardpuig/ubuntu-cleaner","what_it_cleans":"APT deb cache, apt autoremove candidates, old kernel packages, residual package configs, thumbnail cache, browser caches (Chrome/Edge/Mozilla/Opera), and a few app-specific caches (Software Center, Empathy, Google Earth)"},{"language":"Python","name":"BleachBit","safety":"Mandatory Preview before Delete in GUI workflow; per-cleaner options and optional warning elements; CLI preview; can abort when target app is running; shred/truncate options for sensitive data; whitelist-style only-selected cleaners run; no blanket full-disk heuristic deletes","stars_approx":"6.5k","steal":"Separate rule language (CleanerML) with Preview mode, XSD validation, running-process guards, and deep-scan batching for sparse junk under $HOME","technique":"Declarative CleanerML rules DB: search modes file/glob/walk.files/walk.all/walk.top/deep plus regex filters; actions delete/truncate/shred/process/sqlite.vacuum/apt.autoclean/apt.autoremove/yum.clean_all and specialized browser cleaners; OS and running-process guards; loads cleaners from system and personal dirs","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Cross-platform junk and privacy data: browser caches/cookies/history, temp files, logs, package-manager leftovers (apt/yum actions), SQLite vacuum targets, and many app-specific paths via bundled CleanerML definitions"},{"language":"XML","name":"CleanerML (BleachBit cleaners pack)","safety":"Two-tier pipeline: pending must be human-verified before promotion to release; XSD/make tests catch invalid XML; safety inherits BleachBit runtime (preview, options, warnings); no execution engine in-repo","stars_approx":"189","steal":"Split cleaner knowledge into a versioned rules pack with pending vs release lanes and maintainer test annotations instead of hardcoding every path in app code","technique":"Pure rules repository: CleanerML XML packs organized as pending/ vs release/; consumers copy into BleachBit personal_cleaners_dir; Makefile XSD tests and pretty-print; metadata headers (@url, @tested, @note) document verification","url_or_slug":"bleachbit/cleanerml","what_it_cleans":"Bonus/community cleaner definitions for apps and Linux paths not always shipped in core BleachBit (release pack) plus pending definitions awaiting review"},{"language":"C++","name":"Sweeper","safety":"Checkbox list of discrete actions with persisted selection in kprivacyrc; GUI confirmation dialog ('potentially valuable data') unless --automatic; favicon cleaner allowlists icons still referenced by Konqueror bookmarks; conditional actions only if services (klipper/ActivityManager) are registered","stars_approx":"8 (GitHub mirror; canonical invent.kde.org/utilities/sweeper)","steal":"Desktop-integration cleaners that call official D-Bus/KConfig APIs and preserve bookmark-referenced favicons instead of only deleting cache directories","technique":"Hardcoded PrivacyAction classes per target: QDir file removal for thumbnail/favicon caches; KConfig group edits; D-Bus calls to kcookiejar/klipper/Konqueror; KActivities::Stats forgetResources queries; kio_http_cache_cleaner process for web cache","url_or_slug":"KDE/sweeper","what_it_cleans":"KDE/Plasma privacy traces: thumbnails, recent documents/apps, KRunner history, Klipper clipboard history, cookies and cookie policies, Konqueror/web history and HTTP cache, form completions, favicons (preserving bookmark-referenced icons)"},{"language":"Swift","name":"Pearcleaner","safety":"Full Disk Access + privileged helper for system folders; user review of related files; directory include/exclude; Trash-oriented removal rather than silent system wipe. Source-available fair-code (Apache 2.0 + Commons Clause), not pure OSI MIT.","stars_approx":"14.2k","steal":"Sentinel-style low-RAM watcher that cleans leftovers automatically when an app is moved to Trash.","technique":"Library-path walk with bundle-id + app-name matching, adjustable search sensitivity, include/exclude directories; Finder extension and Sentinel watches Trash to auto-find leftovers when apps are discarded.","url_or_slug":"alienator88/Pearcleaner","what_it_cleans":"App bundles and leftovers (prefs, caches, containers, launch agents), orphaned files from deleted apps, Homebrew/dev env junk, PKG/plugins/services; optional universal-binary lipo and unused translations. Closest popular OSS alternative to closed-source Freemacsoft AppCleaner."},{"language":"Shell/Go","name":"Mole","safety":"mo clean/uninstall/purge --dry-run; persistent whitelist for protected caches; path validation and protected-directory rules; stronger confirmation on higher-risk actions; operations logged under ~/Library/Logs/mole with mo history; analyze can move via Finder Trash.","stars_approx":"61.9k","steal":"Persistent cleanup whitelist plus operation history so aggressive default rules remain reviewable and reversible.","technique":"Category rule scanners over known macOS cache/junk roots; interactive uninstall that enumerates related files across many Library locations; TUI plus scriptable mo clean/uninstall/optimize/analyze/purge commands.","url_or_slug":"tw93/Mole","what_it_cleans":"User/system/browser caches, logs and temps, developer caches (Xcode, Node, etc.), Trash, orphaned leftovers of already-uninstalled apps, full app uninstall with remnants, installer files, and project build artifacts."},{"language":"Swift","name":"PureMac","safety":"Trash-only via FileManager.trashItem; explicit review before delete; high-risk system paths hard-excluded; allow-list roots for cleaning; symlink resolve + re-resolve before unlink (TOCTOU); Apple system apps cannot be uninstalled; optional scheduled clean with reclaim threshold.","stars_approx":"5.5k","steal":"Strict/Enhanced/Deep leftover-match sensitivity tiers so users trade recall vs false positives without rewriting rules.","technique":"10-level leftover matching (bundle ID, team ID, entitlements, Spotlight metadata, containers, company-name heuristics); 120+ filesystem search locations; 25 per-app edge-case conditions; parallel category scanners.","url_or_slug":"momenbasel/PureMac","what_it_cleans":"Full app uninstall + orphan finder; system/user caches, logs, temp; Xcode, Homebrew, Node, Docker caches; mail attachments; large/old files; AI app caches. Open MIT CleanMyMac-class GUI (Sensei has no OSS core)."},{"language":"Swift","name":"Mac Sai","safety":"Protected-path blocklist; trash-first; dry-run; firmlink canonicalization; pre-scan cleanability filter drops untrashable items; TOCTOU re-resolve; orphan cleanup limited to caches/logs; chunked cancelable cleans; in-app activity log.","stars_approx":"1.4k","steal":"Pre-scan cleanability filter so the UI never offers items the process cannot actually trash (root-owned or data-vault paths).","technique":"Modular scanners with APFS URLResourceKey prefetch and FSEvents incremental updates; 10-level uninstaller over 17+ Library subdirs; progressive duplicate detection (size, partial SHA-256, full hash, inode); SMAppService + XPC privileged helper.","url_or_slug":"iliyami/MacSai","what_it_cleans":"16 system-junk categories, mail attachments, Trash, browser privacy data, malware signatures, large/old files, duplicates, complete app uninstall leftovers, universal binary thinning. Closest OSS peer to commercial Sensei/CleanMyMac suites."},{"language":"Swift","name":"Dusty","safety":"Allowlist-only deletes gated by single SafetyValidator; personal folders blocked; no symlink following; dry-run option; Trash + short Undo; JSONL deletion log; skips caches for running apps; CLI requires --yes to delete.","stars_approx":"65","steal":"Data-driven cleanup targets: one registry entry drives scan, UI, and safety authorization (prefer allowlist over unrestricted heuristics).","technique":"Fixed CleanupTargetRegistry of path templates only (no open-ended walks); concurrent size scan; age/mtime heuristics for deep project artifacts requiring a sibling project manifest; CLI dusty scan/clean and Shortcuts actions.","url_or_slug":"yagcioglutoprak/dusty","what_it_cleans":"Menu-bar junk cleaner: Safe (user/browser/app caches, logs, Trash), Developer (Xcode DerivedData/simulators, package-manager caches), Deep (old DMG/PKG installers, archives, local TM snapshots, stale project node_modules/target/venvs). Not a full AppCleaner uninstaller."},{"language":"C#","name":"Bulk Crap Uninstaller","safety":"User multi-select before uninstall; leftover cleanup is review-oriented not fully blind; force-uninstall is explicit; portable/settings-in-one-file; not a free-space wipe tool","stars_approx":"20.5k","steal":"Product-scoped leftover scanner that runs after uninstall (files + registry tied to the removed app identity) instead of only global temp globs","technique":"Enumerates uninstall registry + Store/Steam/installer APIs; special handlers per installer engine; leftover and orphan detection via path/registry heuristics keyed to uninstalled products; batch lists and bcu-console automation","url_or_slug":"BCUninstaller/Bulk-Crap-Uninstaller","what_it_cleans":"Unwanted Windows programs in bulk, leftover files/registry after uninstall, orphaned apps, empty/unused Program Files folders; supports Store, Steam, Features, MSI/NSIS/Inno"},{"language":"Python","name":"BleachBit","safety":"Mandatory Preview dry-run listing files before delete; per-cleaner opt-in toggles; shred is optional; no silent full-disk wipe without user selection","stars_approx":"6.5k","steal":"First-class dry-run that enumerates every target path and size before any unlink, with independent enable flags per cleaner","technique":"Declarative cleaner definitions (CleanerML/XML under cleaners/) plus optional winapp2.ini flavors; path rules per application; Preview scans then Delete; CLI for scripting","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Browser/app caches, cookies, history, temp files, logs, clipboard, Windows junk; privacy data; optional file shred and free-space wipe (Windows + Linux)"},{"language":"C#","name":"FluentCleaner","safety":"Nothing runs unless user selects entries; rules are inspectable path-specific entries not drive-wide wildcards; permanent delete (user warned to backup); scheduled/silent mode uses prior selection only","stars_approx":"4.7k","steal":"Thin native engine + multi-database winapp2 parser so community rules update independently of the app binary","technique":"Shared FluentCleaner.Core engine parses winapp2.ini (and custom DBs); Analyze then clean; exact path/reg targets per entry; /AUTO silent clean from saved selection","url_or_slug":"builtbybel/FluentCleaner","what_it_cleans":"Windows app and browser caches, temps, logs, and targeted registry traces described by winapp2-style databases; modern CCleaner-class junk clean"},{"language":"C++","name":"WinDirStat","safety":"User-driven selection for delete/move; Explorer context menu and Recycle Bin paths; cloud-file safeguards and hardlink-aware sizing; no automatic whole-disk junk deletion","stars_approx":"3.8k","steal":"Treemap + largest-files view as the primary UI for deciding what to remove before any rule-based cleaner runs","technique":"Filesystem walk with multithreading and fast NTFS scanning; interactive treemap + extension breakdown; optional content-hash duplicate detection; filters (size, path, reparse points, cloud files)","url_or_slug":"windirstat/windirstat","what_it_cleans":"Disk space hogs (large files/folders), duplicates, empty folders; assists manual cleanup and launches Windows maintenance (Disk Cleanup, DISM, etc.). OSS stand-in for proprietary WizTree/TreeSize"},{"language":"INI/C#","name":"Winapp2","safety":"Entries typically default-off in host UIs; explicit inspectable targets over sweeping globs; Winapp3 labeled power-user/aggressive; host still performs delete with its own confirmations","stars_approx":"1.0k","steal":"Flavor + trim pipeline: one shared rules corpus, generate host-specific subsets and only load entries that match software present on the machine","technique":"Massive winapp2.ini ruleset (app section → FileKey/RegKey paths); automated flavors per host tool; winapp2ool trims to installed software and patches host configs","url_or_slug":"MoscaDotTo/Winapp2","what_it_cleans":"Not a cleaner binary: community database of declarative routines targeting per-app temp/cache/log/MRU/registry junk consumed by CCleaner, BleachBit, FluentCleaner, System Ninja, etc."},{"language":"Python","name":"BleachBit","safety":"GUI Preview before delete, per-option toggles, warnings on risky options (e.g. dnf autoremove), CLI help for non-interactive use","stars_approx":"6.5k","steal":"Ship a small XML/YAML cleaner definition format: each PM gets native commands + path globs, OS filters, and 'tool is running' checks","technique":"Declarative CleanerML rules DB that maps options to package-manager commands (apt.clean, yum.clean_all, dnf.clean_all) and path walks; running-process guards for apt-get/synaptic","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Package-manager caches for APT (clean/autoclean/autoremove + /var/lib/apt/lists), yum, DNF, and pacman, plus many app caches via CleanerML definitions"},{"language":"Shell","name":"pacman-contrib (paccache)","safety":"Explicit op required: -d dryrun, -r remove, or -m move to another dir; sudo only when write needed; reports candidates and disk space saved","stars_approx":"48","steal":"Version-retention policy (keep last K versions per package) plus dry-run and move-aside instead of always unlinking the whole cache","technique":"Walk cache dirs, parse package filenames into name/version/arch, sort with pacsort, keep N newest per package; optional atime/mtime floors; whitelist/blacklist; --uninstalled via pacman -Qq","url_or_slug":"archlinux/pacman-contrib","what_it_cleans":"Pacman package cache (CacheDir from pacman.conf, typically /var/cache/pacman/pkg): old .pkg.tar* versions and matching .sig files"},{"language":"Python","name":"Ubuntu Cleaner","safety":"GUI category selection before clean; privileged deletes go through daemon proxy rather than unconstrained root walks","stars_approx":"256","steal":"Per-PM plugin interface (discover -> list sized cruft -> clean via privileged helper) so the UI never holds root","technique":"Janitor plugin model: AptCachePlugin scans fixed root_path with *.deb pattern and deletes named files via a privileged DBus proxy","url_or_slug":"gerardpuig/ubuntu-cleaner","what_it_cleans":"APT archive cache (/var/cache/apt/archives/*.deb), unneeded packages, old kernels, old installers, browser and thumbnail caches"},{"language":"Python","name":"mac-cleanup-py","safety":"--dry-run, interactive with_prompt on destructive modules, --force to skip warnings, configure screen to enable/disable modules","stars_approx":"2.4k","steal":"Prefer calling the PM's own cleanup API then wiping residual cache from the PM-reported path over hardcoding cache directories only","technique":"Modular collectors that detect if brew (or other tools) exist, invoke the package manager CLI for real cleanup, then delete the resolved cache path from brew --cache","url_or_slug":"mac-cleanup/mac-cleanup-py","what_it_cleans":"Homebrew download/cache (brew cleanup -s, brew --cache path, brew tap --repair) and many language-tool package caches (npm, yarn, pnpm, gem, pod, etc.)"},{"language":"C#","name":"Chocolatey CLI (choco)","safety":"list before remove; --expired partial clean; --noop/--whatif; elevation required for system HTTP cache","stars_approx":"11.5k","steal":"Split list vs remove vs remove-expired, and separate user vs system cache by privilege level","technique":"Package-manager-owned cache API (list stats, remove all, remove --expired) with elevation gating for system vs user cache contexts","url_or_slug":"chocolatey/choco","what_it_cleans":"Chocolatey HTTP download cache (user always; system HTTP cache when elevated) via choco cache list/remove; note licensed edition historically covers install-file cleanup beyond FOSS HTTP cache"},{"language":"TypeScript","name":"npkill","safety":"Per-item confirm via TUI; dry-run; --exclude and --exclude-sensitive; warns with icon on system-sensitive node_modules; --delete-all needs -y to skip warning; default is unlink/rm not Trash","stars_approx":"9400","steal":"Interactive list sorted by size/age with sensitive-path highlighting before any delete","technique":"Low-level filesystem walk for directory name targets (profiles or -t); computes size and last workspace mtime; optional regex filter; interactive multi-select","url_or_slug":"voidcosmos/npkill","what_it_cleans":"node_modules (and other named target dirs) under a scan root; npm/yarn/pnpm project dependency trees, not global package stores"},{"language":"Rust","name":"cargo-cache","safety":"Default is report-only; --dry-run; --autoclean preserves archives and bins needed for rebuild; --info documents what is safe; selective --remove-dir categories","stars_approx":"989","steal":"Layered cache model: keep original archives, drop extracted checkouts, optional size-cap trim and unreferenced-crate purge","technique":"Knows cargo home layout; size breakdown by component; --autoclean removes re-extractable checkouts only; clean-unref vs Cargo.toml graph; age filters; trim to size cap; query/top-N","url_or_slug":"matthiaskrgr/cargo-cache","what_it_cleans":"Cargo home cache (~/.cargo or $CARGO_HOME): registry crate archives, extracted sources, git db/checkouts; optional local target/ and sccache stats"},{"language":"Rust","name":"kondo","safety":"Interactive prompt per project (rm -rf style); warns it is essentially destructive delete; no Trash; age filter reduces accidental wipe of active work","stars_approx":"2400","steal":"Project-type fingerprint table plus mtime age gate so only stale project artifacts are offered","technique":"Recursive walk + project-type heuristics (fingerprint files/dirs per ecosystem); optional --older duration mtime filter; multi-path roots","url_or_slug":"tbillington/kondo","what_it_cleans":"Per-project build/deps junk for 20+ types: Node node_modules, Cargo target, Gradle/Maven build dirs, Python/Pixi/Jupyter artifacts, .NET, Unity, etc."},{"language":"Python","name":"mac-cleanup-py","safety":"--dry-run; --force accepts warnings; interactive module configuration; not full-disk walk, only module-defined targets","stars_approx":"2400","steal":"One module per toolchain with dry-run and user-configurable enable/disable registry","technique":"Plugin-style default modules that call each tool's cache-clean path/API or known cache dirs; user can enable/disable modules and add custom modules","url_or_slug":"mac-cleanup/mac-cleanup-py","what_it_cleans":"Modular macOS caches including npm, yarn, pnpm, poetry, go, gradle (also bun, gem, nuget, xcode, docker, brew)"},{"language":"Rust","name":"oxiclean","safety":"--dev excluded from --all; --dry-run; --yes; never touches ~/.cargo/bin, poetry venvs, Gradle wrapper dists, npm globals; avoids hardlink-breaking store wipes; JSON mode for scripts","stars_approx":"24","steal":"Safe-vs-deep tiering with package-manager-native prune instead of blind rm of hardlinked stores","technique":"Known cache path inventory + tool-aware commands (e.g. pnpm store prune, package manager clean); labels safe vs re-download; --deep for aggressive; scans then shows table before action","url_or_slug":"croaky-fx/oxiclean","what_it_cleans":"Opt-in --dev language caches: npm/yarn/pnpm/bun/deno, pip/uv/poetry/conda, cargo registry/git, go modules, Gradle/Maven, NuGet, ccache (plus Linux system caches)"},{"language":"Shell","name":"docker-gc","safety":"DRY_RUN=1; exclude files for images/containers/volumes; grace period; MINIMUM_IMAGES_TO_SAVE; does not strip tags from images still referenced by containers; archived (prefer modern prune for new work)","stars_approx":"5.0k","steal":"Grace period + exclude-list files + keep-N-most-recent-images-per-repo so aggressive GC never deletes the only local base image you rebuild against daily","technique":"Docker CLI inventory: list exited containers by exit age (GRACE_PERIOD_SECONDS), remove them, then rmi images not used by remaining containers; optional volume prune; exclude lists via grep patterns on files","url_or_slug":"spotify/docker-gc","what_it_cleans":"Exited containers past grace period, unreferenced images (and optionally dangling volumes); classic Docker host GC for cron/CI"},{"language":"Shell","name":"docker-clean","safety":"--dry-run lists targets without deleting; per-resource flags so you can prune containers without touching tagged images; empty-networks-only for network clean","stars_approx":"1.3k","steal":"Compose cleanup as named intensity tiers (safe default vs images vs all) with a first-class dry-run that prints the same plan the delete path would execute","technique":"Thin shell wrapper composing docker ps/images/volume/network queries and rm/rmi; mode commands (run/stop/images/all) select which object classes run","url_or_slug":"ZZROTDesign/docker-clean","what_it_cleans":"Stopped containers, untagged/dangling images, dangling volumes, empty networks; optional full wipe of containers/images/volumes and daemon restart"},{"language":"Rust","name":"docuum","safety":"--keep REGEX allowlist; --min-age floor; never deletes images in use by containers; threshold-based instead of blind TTL so frequently used old images survive","stars_approx":"705","steal":"Space-budget cleaner driven by real last-use from engine events, not created-at, with persisted LRU state so restarts do not reset policy","technique":"Subscribes to docker system events to track last-use timestamps (persisted locally); on events/start runs LRU eviction; deletes children before parents; threshold as absolute size or % of disk","url_or_slug":"stepchowfun/docuum","what_it_cleans":"Docker images only, until total image disk usage is under a configured threshold (daemon-style continuous cleaner for CI/dev hosts)"},{"language":"Python","name":"docker-custodian","safety":"Never removes running containers or images used by any container; --exclude-image / --exclude-image-file and --exclude-container-label with wildcard patterns","stars_approx":"374","steal":"Label- and tag-pattern allowlists for compose projects so a host-wide age prune cannot delete stopped debug containers marked keep=true","technique":"Docker Engine API via Python: filter by container/image age (pytimeparse max-age), skip running containers and images still referenced; pattern match on tags and labels for excludes","url_or_slug":"Yelp/docker-custodian","what_it_cleans":"Stopped containers and unused images older than max ages (dcgc); optionally stops long-running containers matching a name prefix (dcstop)"},{"language":"Shell","name":"docker-reclaim-disk-space","safety":"Interactive confirmation by default; -y to skip; --no-restart to avoid engine bounce; prints usage before and after so reclaim is measurable","stars_approx":"39","steal":"Two-phase reclaim: prune container objects, then compact the host VM disk image and re-measure df, because prune alone often does not free host space on Docker Desktop/Colima-style VMs","technique":"docker system df before/after; sequential docker prune-style removals; platform-specific sparse VM shrink for Docker.raw; launchctl/systemctl restart and wait-until-ready","url_or_slug":"samoshkin/docker-reclaim-disk-space","what_it_cleans":"Stopped containers, orphan/dangling image layers, unused volumes, builder cache, plus Docker Desktop Mac VM disk (Docker.raw) after prune; restarts engine"},{"language":"TypeScript","name":"npkill","safety":"Interactive select (Del/Space); dry-run; exclude/exclude-sensitive; warning highlight for system-critical installs; --delete-all needs -y to skip warning","stars_approx":"9400","steal":"TUI multi-select with range mode, sort by size/path/age, and sensitive-path warnings before bulk delete","technique":"Fast filesystem walk from cwd/home for directory name targets; sizes folders; last workspace mtime; profiles/targets config; optional JSON/JSON-stream output","url_or_slug":"voidcosmos/npkill","what_it_cleans":"Scattered node_modules (default); extensible targets/profiles for other named dirs like .cache"},{"language":"Swift","name":"DevCleaner (xcode-dev-cleaner)","safety":"Inspect-before-clean UI; Device Support defaults exclude latest OS version per platform; backup warning; relies on internal folder layouts","stars_approx":"1600","steal":"Per-category defaults that keep newest Device Support while offering older versions for reclaim","technique":"Inspect known undocumented Xcode layout under Developer/Xcode paths; category UI; optional periodic scan reminder; CLI for scripted cleans","url_or_slug":"vashpan/xcode-dev-cleaner","what_it_cleans":"Xcode Device Support symbols, Archives, DerivedData, documentation cache, old simulator/device logs, legacy offline docs"},{"language":"Rust","name":"kondo","safety":"Prompt before delete (essentially rm -rf with confirm); quiet/all modes; no trash by default; docs stress backups","stars_approx":"2400","steal":"Project-type fingerprint table (manifest marker -> list of cleanable artifact paths) reusable as a rules engine","technique":"Walk trees; detect project type via manifests; map type to artifact dirs; optional --older mtime filter; CLI + GUI","url_or_slug":"tbillington/kondo","what_it_cleans":"Project-local deps/build artifacts: node_modules, Cargo target, Gradle/Maven build dirs, Flutter/Pub, Unity/Unreal, .NET bin/obj, and 20+ project types"},{"language":"Swift","name":"ClearDisk","safety":"Safe/Caution/Risky labels; move to Trash only; Xcode-running warning; no network/telemetry; age-based suggestions","stars_approx":"560","steal":"Risk-tiered known-path catalog with one-line rebuild explanations and DerivedData-per-project size via info.plist","technique":"Rules DB of known absolute paths scanned on a timer; project-folder walk for type-specific artifacts; DerivedData broken down by reading info.plist; risk + human descriptions","url_or_slug":"bysiber/cleardisk","what_it_cleans":"63+ global dev caches (Xcode DerivedData/Archives/simulators, JetBrains, Gradle, Maven, npm/pnpm, Docker, Flutter, VS Code, AI tool caches) plus per-project artifacts across 23 project types"},{"language":"Swift","name":"AndroidStudioCacheCleaner-mac","safety":"Toggle which global items to clean; Clean All requires confirmation; native macOS app (user-driven)","stars_approx":"small","steal":"Two-layer model: shared global tool caches vs project-local clean that can invoke official clean commands when available","technique":"Known global cache paths with size refresh; scan user-chosen tree for pubspec.yaml / build.gradle|settings.gradle; shell out to flutter clean for Flutter projects","url_or_slug":"Blaze414/AndroidStudioCacheCleaner-mac","what_it_cleans":"Android Studio caches/Application Support, ~/.gradle/caches, Flutter pub-cache; per-project Flutter (flutter clean) and Kotlin/Gradle build/ dirs"},{"language":"Python","name":"BleachBit","safety":"GUI Preview before delete; selective option toggles; CLI; optional shred; cookie allowlist keeps chosen sites signed in while wiping other junk","stars_approx":"6500","steal":"Ship browser junk knowledge as external CleanerML/YAML rules (not hard-coded paths) so profiles and new Chromium forks can be added without code changes","technique":"Declarative CleanerML rules DB (path globs, SQLite ops) over known profile dirs; multi-cleaner options; cookie keep-list manager for selective domain retention","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Browser cookies, history, cache, IndexedDB, GPU/code/shader caches, crash reports for Chrome/Firefox/Edge/Brave/Opera/Vivaldi/Zen and many non-browser apps; system temps/logs"},{"language":"TypeScript","name":"Cookie AutoDelete","safety":"Auto-clean opt-in; whitelist/greylist allowlists; manual popup clean; config import/export; no blind global wipe of trusted domains","stars_approx":"2000","steal":"Two-tier policy model (permanent whitelist + session greylist) so privacy cleaning never logs users out of intentionally trusted sites","technique":"WebExtension cookies + browsingData APIs; tracks open tabs/containers; matches domains against whitelist (keep forever) and greylist (until restart)","url_or_slug":"Cookie-AutoDelete/Cookie-AutoDelete","what_it_cleans":"Cookies and optional site data (localStorage, IndexedDB, cache, plugin data) when tabs close, domains change, or the browser restarts"},{"language":"JavaScript","name":"Cookie-Editor","safety":"Fully manual user-driven edits; mass-delete is an explicit UI action; no automatic background wiping","stars_approx":"1600","steal":"First-class Safari/WebKit packaging with per-browser manifests so macOS/iOS Safari cookie cleanup is not a Chromium-only afterthought","technique":"Extension cookies API scoped to active tab origin; separate manifests and Safari/Xcode packaging for WebKit vs Chromium vs Gecko","url_or_slug":"Moustachauve/cookie-editor","what_it_cleans":"Cookies for the current tab (create, edit, delete, mass-delete for that site); Chrome, Firefox, Safari, Edge, Opera including mobile Safari/Firefox"},{"language":"PowerShell","name":"mTTCleaner","safety":"-WhatIf dry-run; confirmation unless -Automated/-SkipConfirmation; per-browser target; skip DB compaction; operation logging","stars_approx":"5","steal":"Detect running browsers, close them, then clean profile paths with dry-run size preview so multi-profile disk clean avoids locked SQLite files","technique":"Platform-specific path maps plus install detection (registry/mdfind/which/flatpak/snap); stop browser processes; delete known cache dirs; SQLite VACUUM; glob *metrics*/*telemetry*","url_or_slug":"mytech-today-now/mTTCleaner","what_it_cleans":"Disk caches (Cache, Code Cache, GPU Cache, cache2, startupCache), SQLite compaction of cookies/history/web data DBs, and metrics/telemetry files across 28+ browsers including Safari"},{"language":"Vue","name":"Cache Cleaner","safety":"User picks data types and site vs global scope; no background auto-delete; preferences stored locally","stars_approx":"12","steal":"Site-scoped vs global clear matrix with one-key clear-and-reload for iterative privacy sweeps without quitting the browser","technique":"Cross-browser browsingData API with Firefox vs Chromium branching; hotkey (default F4); popup, side panel, and context menu triggers","url_or_slug":"cssnr/cache-cleaner","what_it_cleans":"User-selected cache and browsing data for the current site or the whole browser; Firefox Cache Storage (service worker) workaround; optional page reload after clear"},{"language":"C","name":"logrotate","safety":"Debug/dry-run mode (-d) prints planned actions without rotating; state file avoids double-rotation; policies are explicit per path; does not invent targets outside config; careful with copytruncate vs rename for open files.","stars_approx":"1500","steal":"Per-path policy objects with OR triggers (size OR age) plus rotate-N retention and a durable state file so cleaners are idempotent across scheduled runs.","technique":"Declarative rules DB (/etc/logrotate.conf + /etc/logrotate.d/*): match log globs, trigger on size, interval (hourly..monthly), minutes elapsed, or count; keep N rotations; optional compress, copytruncate, create mode/owner, sharedscripts, prerotate/postrotate hooks; state file tracks last action per log.","url_or_slug":"logrotate/logrotate","what_it_cleans":"Text/binary log files under admin-defined paths: rotates, compresses, mails, and removes old generations for daemons that keep writing unbounded logs."},{"language":"Shell","name":"log2ram","safety":"Hard SIZE cap prevents RAM fill; NOTIFICATION when space is insufficient; journal vacuum recommended to a fraction of SIZE before first start; uninstall restores disk layout; no blind recursive wipe of non-log user data.","stars_approx":"3100","steal":"Journal-aware capacity guard: before committing logs to a constrained store, call journalctl vacuum (or equivalent) so retained journal cannot exceed the log volume budget.","technique":"systemd unit mounts size-capped tmpfs over log dirs; daily/timer sync via rsync or cp; JOURNALD_AWARE path runs journal rotation/vacuum before sync; optional zram with COMP_ALG; SIZE/LOG_DISK_SIZE caps capacity.","url_or_slug":"azlux/log2ram","what_it_cleans":"Does not bulk-delete logs; maintains /var/log (and optional PATH_DISK paths) on tmpfs/zram so writes stay in RAM, then syncs to disk on timer/shutdown. Journal-aware path vacuum-sizes systemd journal so it fits the RAM budget."},{"language":"Rust","name":"oxiclean","safety":"--dry-run preview, --yes non-interactive, --json for automation, --skip to exclude journal from --all; refuses personal docs/downloads; sudo only for privileged ops; deep mode opt-in and discouraged for cron.","stars_approx":"24","steal":"First-class journal module: print current journal disk-usage, vacuum with explicit size/time policy, report bytes freed, expose as a single flag usable alone or in --all with skip.","technique":"Distro detect from /etc/os-release + PATH tools; for journals shells out to journalctl --disk-usage then vacuum (size/time semantics of journald); measures directory sizes before/after for freed-space reporting; modular flags rather than path walking for journals.","url_or_slug":"croaky-fx/oxiclean","what_it_cleans":"Among broader Linux cleanup: systemd journal archives via vacuum; also package caches, trash, coredumps. Explicit -j/--journal module for log maintenance."},{"language":"Shell","name":"mac-cleanup-sh","safety":"--dry-run estimates space and prompts (Enter) before real run; real path uses rm -rf (no trash); requires upfront sudo; no fine-grained allowlist beyond the fixed path list; aggressive system cache paths included, so dry-run is essential.","stars_approx":"2800","steal":"collect_paths / remove_paths batching: accumulate every candidate, optionally sum du in dry-run, then delete once, so macOS log modules share one confirm and one space report.","technique":"Hardcoded path globs collected into path_list then batch-removed; dry-run uses sudo du -ck to sum reclaimable size; no package APIs for logs, pure filesystem walk of known log roots.","url_or_slug":"mac-cleanup/mac-cleanup-sh","what_it_cleans":"macOS Apple System Logs (/private/var/log/asl/*.asl), /Library/Logs/DiagnosticReports, Mail/CoreSimulator logs, plus many app logs (JetBrains, Steam, Minecraft, Teams, etc.). Note: classic ASL focus; not a full unified-log (log show) vacuum tool. Repo marked DEPRECATED but still the common OSS reference for ASL cleanup paths."},{"language":"TypeScript","name":"privacy.sexy","safety":"recommend tiers (standard/strict/null); transparent human-readable docs per tweak; admin elevation check; service stop/start state files; reversible where designed; user selects scripts (not silent wipe-all); no trash vs unlink distinction (direct delete after privilege fix).","stars_approx":"5900","steal":"Locked-log pattern: stop owning service -> grant ACL if needed -> delete CBS/ETL globs -> restart service; plus wevtutil enumerate-and-clear as a separate Event Viewer module with documented risk tiers.","technique":"Declarative YAML collection (windows.yaml): named actions with file/directory globs, StopService/StartService around locked files, grantPermissions for SYSTEM-owned ETL, raw batch with wevtutil for event logs; templating expands portable scripts; docs cite Microsoft/forensic sources per path.","url_or_slug":"undergroundwires/privacy.sexy","what_it_cleans":"Windows log maintenance scripts: %SYSTEMROOT%\\Logs\\CBS\\CBS.log, DISM.log, Temp\\CBS, waasmedic, NetSetup; DiagTrack Event Trace Logs (AutoLogger/ShutdownLogger *.etl); full Event Viewer clear via wevtutil el/cl. Closest OSS catalog for CBS/ETL-style cleanup when proprietary cleaners have no open core."},{"language":"Swift","name":"Pearcleaner","safety":"User reviews matched files before removal; Full Disk Access + privileged helper for system paths; source-available (Apache 2.0 + Commons Clause)","stars_approx":"14166","steal":"Background Trash watcher that auto-offers leftover cleanup right after a manual app delete","technique":"Bundle metadata + Library path walks with tunable sensitivity/include-exclude dirs; Sentinel Monitor FS watcher re-scans when apps hit Trash","url_or_slug":"alienator88/Pearcleaner","what_it_cleans":"macOS apps plus related prefs/caches/containers/launch agents; orphaned leftovers after manual Trash deletes; Homebrew/PKG/plugin remnants"},{"language":"C#","name":"Bulk Crap Uninstaller","safety":"Reviewable leftover lists; bulk/unattended modes with confirmations; Apache 2.0","stars_approx":"20536","steal":"Two-phase uninstall: run the vendor uninstaller first, then residual scan using the same app identity (name/publisher/path)","technique":"Registry Uninstall entries + multi-installer APIs; orphaned app detection; leftover scan after batch uninstall keyed to app identity","url_or_slug":"BCUninstaller/Bulk-Crap-Uninstaller","what_it_cleans":"Windows apps (MSI/NSIS/Inno/Store/Steam/Features) and post-uninstall leftovers including files, registry keys, and residual app data"},{"language":"Shell","name":"Mole","safety":"mo clean/uninstall --dry-run; whitelist for protected caches; operation log/history; analyze uses Trash; confirmations on risky actions","stars_approx":"61915","steal":"Separate clean modes for still-installed vs already-gone apps, with short-name floors so tokens never match half of Library","technique":"Bundle-ID or app-name variant matching across 20+ Library locations; rejects generic/short names; skips broad Preferences/ByHost; protected-path checks","url_or_slug":"tw93/Mole","what_it_cleans":"Installed apps + related leftovers; already-uninstalled app leftovers via mo clean; caches, logs, installer artifacts"},{"language":"Python","name":"MacCleaner","safety":"Full list before delete; all removals via Finder Trash (osascript); skips com.apple.*; per-row selection","stars_approx":"1","steal":"Orphan residual-plist pass: inventory installed bundle IDs, then flag reverse-DNS prefs/support files with no living owner","technique":"Parse CFBundleIdentifier from each .app Info.plist; precise bundle-ID prefix or exact app-name match; reverse orphan scan for com.*/io.*/org.* with no installed owner","url_or_slug":"dheerajbatra/MacCleaner","what_it_cleans":"Per-app Library remants (Preferences plists, Application Support, Caches, Containers, LaunchAgents/Daemons, cookies/WebKit); orphaned prefs after apps deleted"},{"language":"PowerShell","name":"MS-Scripts Find-AppLeftovers","safety":"Report-only, no auto-delete; explicit manual-review warnings for shared/system folders","stars_approx":"7","steal":"Normalize display names and folder names before fuzzy match, then sort leftovers by size to prioritize reclaimable space","technique":"Installed set from Uninstall registry (HKLM/HKCU/Wow6432Node) + Appx; normalize names to alnum; walk AppData dirs; flag unmatched folders; size ranking","url_or_slug":"koki7o/MS-Scripts","what_it_cleans":"Orphaned folders under %LOCALAPPDATA% and %APPDATA% left by uninstalled Windows apps (identify/report only)"},{"language":"Go","name":"CrunchyCleaner","safety":"CLI -d dry-run/simulation, interactive TUI multi-select, optional -a auto-all; docs say cache/temp only","stars_approx":"141","steal":"Single rules table mapping each app to multiple platform-specific globs including Flatpak variants","technique":"Hardcoded per-OS path rules DB (cc_caches.go) with env vars, profile wildcards, and Flatpak sibling paths","url_or_slug":"Knuspii/CrunchyCleaner","what_it_cleans":"Steam appcache/htmlcache, Discord Cache/Code/GPUCache, Spotify Storage, Slack Cache/Code/GPUCache, browsers, JetBrains, GPU shader caches; Flatpak paths on Linux"},{"language":"PowerShell","name":"WindowsClearCache","safety":"-DryRun with file/byte preview; IncludeUsers/ExcludeUsers; skips AAD Broker and MSTeams packages to protect sign-in; never deletes Outlook OST/PST","stars_approx":"32","steal":"Chromium template reuse for Electron apps plus protected-package allowlist so auth-bearing AppX caches are not wiped","technique":"Shared Chromium cache-subdir template applied to Electron apps; per-app known relative paths under each user profile; multi-user scan of C:\\Users","url_or_slug":"inode64/WindowsClearCache","what_it_cleans":"Microsoft Teams (classic + MSTeams Store package), Slack, Discord, Steam htmlcache, browsers, Epic, OneDrive, Office, AppX package caches, Windows temp/prefetch/WU"},{"language":"Lua","name":"spotify-cleaner","safety":"Threshold gate is the primary guard (below limit = no delete); logs keep/clear decisions; settings.conf for custom paths; uninstall removes cron","stars_approx":"2","steal":"Per-app soft size cap: only clear large media caches when they exceed a configured budget","technique":"Measure directory size; if >= user threshold (default 150MB) run rm -r contents; cron every 5h after install","url_or_slug":"joseareia/spotify-cleaner","what_it_cleans":"Spotify desktop Storage and Data dirs under ~/.cache/spotify (configurable paths)"},{"language":"Shell","name":"Steam-Deck.Shader-Cache-Killer","safety":"dry-run mode (live=0); zenity checklist; extra confirmation before compatdata delete; Proton filtered; USE AT OWN RISK disclaimer","stars_approx":"491","steal":"Offer move/symlink of huge per-game caches to secondary storage instead of only hard-deleting","technique":"du per appid dir; name resolution via libraryfolders.vdf + appmanifest.acf, Steam GetAppList API fallback, Steam log scrape for non-Steam; excludes Proton","url_or_slug":"scawp/Steam-Deck.Shader-Cache-Killer","what_it_cleans":"Steam shadercache and compatdata per appid; companion mover relocates caches to SD/external via copy+symlink"},{"language":"Python/TypeScript","name":"decky-storage-cleaner","safety":"Selective multi-select UI (not blind full wipe); documents shader regen; cloud_status available to reason about save risk on compatdata","stars_approx":"81","steal":"Present junk as named games with human sizes by joining filesystem appid folders to live Steam app metadata","technique":"Walk appid subdirs and sum sizes (skip symlinks); Decky UI maps appid via Steam appStore.GetAppOverviewByGameID for name/type/cloud_status; shutil.rmtree by selection","url_or_slug":"mcarlucci/decky-storage-cleaner","what_it_cleans":"Steam Deck steamapps/shadercache and steamapps/compatdata on a per-appid basis (Steam and non-Steam)"},{"language":"Python","name":"BleachBit","safety":"Preview/dry-run listing before Delete; per-option checkboxes; Windows option warns Explorer will restart; permanent unlink not trash","stars_approx":"6500","steal":"Dedicated cleaner id for preview caches with platform actions, especially kill-shell / delete locked DB globs / restart-shell for Windows","technique":"CleanerML rules DB: unix walk.files on known thumbnail roots; windows process kill explorer + glob-delete thumbcache*.db + relaunch explorer.exe","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"Linux Freedesktop thumbnail dirs (~/.thumbnails, $XDG_CACHE_HOME/thumbnails); Windows Explorer thumbcache*.db under LocalAppData\\Microsoft\\Windows\\Explorer"},{"language":"C++","name":"Thumbcache Viewer","safety":"Read-only by design; docs note DBs are often locked by Explorer and should not be casually deleted while in use","stars_approx":"165","steal":"Treat Windows thumbnails as size-tier database set (thumbcache_*.db), not a walkable image folder; add inspect-before-wipe using format knowledge","technique":"Binary format parsers for multi-size thumb/icon cache databases; GUI and CLI extraction of embedded JPEG/PNG/BMP thumbnails","url_or_slug":"thumbcacheviewer/thumbcacheviewer","what_it_cleans":"Does not wipe live caches; extracts/views Windows Vista-11 thumbcache_*.db and iconcache_*.db (OSS alternative to opaque Disk Cleanup/CCleaner thumbnail handling)"},{"language":"Go","name":"osx-thumbnails","safety":"Read-only reconstruction; copies SQLite to temp before querying so live cache is not written","stars_approx":"<50","steal":"Model Quick Look as index.sqlite + thumbnails.data pair; optional export-then-clear with qlmanage -r cache for safe privacy wipe","technique":"Locate $TMPDIR/../C/com.apple.QuickLook.thumbnailcache; copy index.sqlite; SQL SELECT bitmap offsets/sizes from thumbnails table; slice thumbnails.data into PNGs","url_or_slug":"CoderCookE/osx-thumbnails","what_it_cleans":"Reconstructs images from macOS Quick Look thumbnail cache (forensic/privacy); maps store that closed cleaners wipe via qlmanage or path delete"},{"language":"Python","name":"Vinetto","safety":"Extract-to-output-dir only; no system wipe; quiet/verbose levels and structured exit codes","stars_approx":"22","steal":"Orphan policy: report or delete cache entries whose source file is gone instead of always nuking the entire thumbnail store","technique":"Mode-based scan (file/dir/recursive/automatic Vista+ user Explorer paths); format-aware extract; optional Windows.edb cross-check; HTML reports; planned dir-vs-cache consistency","url_or_slug":"AtesComp/Vinetto","what_it_cleans":"Parses legacy Windows Thumbs.db (OLE) and modern Thumbcache_*.db; optional ESEDB name recovery; targets stale/orphan thumbnail intelligence more than bulk free-space"},{"language":"Batch","name":"Fix white or blank Icons (Windows 10/11)","safety":"No dry-run or trash; user double-clicks scripts; documents access-denied leftovers and post-reboot cleanup","stars_approx":"38","steal":"Second-pass model for locked files (ThumbCacheToDelete + reboot) and separate modes for icon-only vs thumbnail-only vs full Explorer cache wipe","technique":"Hardcoded LocalAppData\\Microsoft\\Windows\\Explorer paths; taskkill explorer.exe; del /f /s /q globs; start explorer; notes ThumbCacheToDelete leftovers until reboot","url_or_slug":"K3V1991/Fix-white-or-blank-Icons-Windows-10-and-11","what_it_cleans":"Windows iconcache*.* and thumbcache*.* (plus optional full Explorer cache folder and Start Menu tile caches)"},{"language":"Python","name":"BleachBit","safety":"Preview/dry-run lists files before delete; per-cleaner checkboxes; shred opt-in; documents SSD/TRIM/HDD limits and rejects multi-pass marketing myths","stars_approx":"6500","steal":"Dual-mode delete (fast unlink vs shred) gated by preference, with Preview listing every path before any irreversible write","technique":"Declarative CleanerML rules DB (paths, globs, app-specific targets) plus Winapp2.ini; shred uses single-pass overwrite plus rename then unlink; free-space wipe fills unused space so deleted remnants are overwritten","url_or_slug":"bleachbit/bleachbit","what_it_cleans":"App caches, browser junk, temp/logs, privacy traces; optional shred of selected files; free/empty disk space; Linux RAM and swap wipe"},{"language":"TypeScript","name":"privacy.sexy","safety":"Full visibility of generated scripts; optional reverse scripts (revertCode); web mode can export without running a local binary; recommendation levels (standard/strict)","stars_approx":"5900","steal":"Model every clean/harden action as an independently executable, documented, optionally reversible script unit instead of a black-box clean button","technique":"Data-driven YAML collections: categories to scripts with inline code/revertCode or shared function calls; templating compiles portable shell/batch scripts from a curated recipe DB (not filesystem junk heuristics)","url_or_slug":"undergroundwires/privacy.sexy","what_it_cleans":"Privacy cleanup (history/telemetry leftovers), debloat, OS data-collection disable, security/privacy hardening scripts on Windows, macOS, and Linux"},{"language":"C","name":"nwipe","safety":"Explicit device selection; --exclude by stable by-id paths; GUI or CLI; warns if optional tools missing; SSD guide documents flash overwrite limits","stars_approx":"1200","steal":"Post-wipe certificate/report (device identity, method, error counts) so users can prove what was erased","technique":"Enumerates disks via parted/smartmontools/hdparm; multi-pass standards (zeros, DoD short/full, Gutmann, Schneier, HMG IS5, PRNG stream, BMB21-2019); parallel multi-disk wipe; optional verify; evolving ATA/NVMe firmware sanitize","url_or_slug":"martijnvanbrummelen/nwipe","what_it_cleans":"Entire block devices (full permanent erase of disks); OSS successor to DBAN dwipe, not a junk-file cleaner"},{"language":"C","name":"THC Secure Delete (srm/sfill)","safety":"No junk dry-run scan (explicit targets only); -f/-v flags; documented limits (disk caches, journals, networks, SSDs). Destructive by design","stars_approx":"11","steal":"Secure-delete pipeline of overwrite then truncate then random rename then unlink so name and size metadata do not survive","technique":"User supplies paths/mounts. Secure path: multi-pass overwrite (38 Gutmann-style default; -l/-ll fewer), truncate, rename, then unlink; large block writes plus sync; free-space wipe creates a fill file until the FS is full then deletes it","url_or_slug":"gordrs/thc-secure-delete","what_it_cleans":"User-chosen files (srm); free disk space (sfill); swap (sswap); unused RAM (smem). Classic secure-delete toolkit widely packaged as secure-delete on distros; low GitHub stars because this is a historical mirror of THC code"},{"language":"C","name":"Raider (File Shredder)","safety":"User-driven selection only (no automatic junk rules); Flathub/GNOME Circle packaging; honest SSD limitations in README","stars_approx":"83","steal":"Ship permanent-erase as a small focused desktop action (pick files, shred, done) separate from bulk junk-cleaner UX","technique":"User picks files in a GTK4 app; overwrites file contents (shred-style) before deletion; documents that SSD wear-leveling makes perfect software shredding impossible","url_or_slug":"ADBeveridge/raider","what_it_cleans":"User-selected sensitive files/folders via GNOME File Shredder UI (permanent erase, not system junk discovery)"},{"language":"Rust","name":"kondo","safety":"Interactive confirmations before delete (guided rm -rf); multi-path roots; symlink/fs options; no trash/recycle by default; strong 'have backups' warnings","stars_approx":"~2350","steal":"Ecosystem detector table: one walk, many project types via markers, plus age filter (only purge trees not modified for N months)","technique":"Recursive directory walk; project-type detection via marker files/heuristics; map type to known artifact dirs; optional --older mtime filter so only stale projects are cleaned","url_or_slug":"tbillington/kondo","what_it_cleans":"Regenerable project dependencies and build artifacts (node_modules, target, build, etc.) across 20+ ecosystems (Cargo, Node, Python, Gradle, Unity, Unreal, Godot, Zig, .NET, ...)"},{"language":"Python","name":"mac-cleanup-py","safety":"--dry-run; --force to accept warnings; interactive module configuration; --verbose lists folders to delete; custom module path support","stars_approx":"~2370","steal":"Plugin-style cleaner modules with a configure screen: one module per app/ecosystem, user-toggleable, dry-run first","technique":"Composable named modules (brew, docker, xcode, npm, ...); each encodes known paths and/or package-manager CLI cleanup calls; configure which modules run","url_or_slug":"mac-cleanup/mac-cleanup-py","what_it_cleans":"macOS trash, system/user caches and logs, Homebrew, Xcode DerivedData/archives/simulators, Docker dangling images/stopped containers, npm/yarn/pnpm, pip/poetry/pyenv, browsers, JetBrains logs, Steam, Telegram, and many other app modules (Python rewrite of mac-cleanup-sh; OSS stand-in for CleanMyMac-class tools)"},{"language":"Go","name":"CrunchyCleaner","safety":"-d simulation mode without deleting; categories labeled Admin/Root where needed; documents cache/temp-only scope; user-risk disclaimer","stars_approx":"~140","steal":"Single maintainable path-template table keyed by OS + env (LOCALAPPDATA, ~/.cache, Flatpak .var/app) for fast cross-platform cache coverage","technique":"Hard-coded OS-specific path rules DB (env vars + globs like Profiles/*/cache2); TUI category selection; optional -a auto-clean all; path existence check then delete contents","url_or_slug":"Knuspii/CrunchyCleaner","what_it_cleans":"Windows/Linux software caches and temp: system temp/logs/thumbnails; browsers (Firefox/Chrome/Edge/Brave/Opera); VS Code/JetBrains/Slack; Steam/Epic; Discord/Spotify/Telegram; Go/pip/npm/yarn/cargo/NuGet/Gradle caches; Flatpak app paths on Linux"},{"language":"Go","name":"moonbit","safety":"clean is dry-run unless --force; high-risk classes excluded by default (browser caches, Steam shaders/downloads, session/project data); backups list/restore; category filtering","stars_approx":"~65","steal":"Dry-run-by-default clean requiring explicit --force, plus quick/deep risk tiers and optional systemd scheduled scan/clean","technique":"Quick vs deep scan modes; category include/exclude filters; package-manager CLI/API cleanup; deep-scan discovery for app cache/log/temp paths; systemd timer and daemon automation","url_or_slug":"Nomadcxx/moonbit","what_it_cleans":"Linux system junk: package-manager caches (pacman/apt/dnf/zypper, AUR helpers), temps, thumbnails, font caches, logs; deep app caches (IDE/Electron/AI/Bottles/Lutris); Docker images/containers/volumes/build cache; Plex/Jellyfin transcodes; orphans/old kernels; optional duplicate finder"},{"language":"Python","name":"sifty","safety":"Dry-run by default (requires --apply); Recycle Bin only via Send2Trash + sifty undo; protected paths (Windows, Program Files, profile root); audit log of applied deletes; AI advisory-only","stars_approx":"~90","steal":"Single trash() path with Recycle Bin + undo + audit log, combined with dry-run default and hard protected-path denylist","technique":"Named junk categories with fixed Windows paths (admin-gated where needed); winget/registry for apps; SHA-256 duplicate scan; separate purge command for dev artifacts","url_or_slug":"Vortrix5/sifty","what_it_cleans":"Windows junk/caches (user/system temp, thumbnails, browser caches, winget cache, crash dumps, Windows Update leftovers, Discord); opt-in Downloads installers and Windows.old; also node_modules/build purge, WSL2 VHD and git worktrees, duplicates, app leftovers after uninstall"},{"language":"Shell","name":"free-disk-space","safety":"Per-category boolean toggles (tool-cache default off); destructive unlink only; safe on ephemeral hosted runners, dangerous if a needed toolchain is toggled on","stars_approx":"703","steal":"Emit bytes-saved-per-category after each purge bucket so operators tune which junk classes are worth removing","technique":"Category rule list of known large paths; rm -rf + apt remove + docker prune; per-category df before/after accounting","url_or_slug":"jlumbroso/free-disk-space","what_it_cleans":"Ubuntu GHA runner bulk: Android/.NET/Haskell toolchains, large apt packages, Docker images, optional AGENT_TOOLSDIRECTORY tool-cache and swap (~up to ~31 GB)"},{"language":"Shell","name":"maximize-build-space","safety":"root-reserve-mb/temp-reserve-mb/swap-size-mb knobs; software removal is opt-in; docs warn reverse-engineered runner layout may break jobs that need removed runtimes","stars_approx":"522","steal":"Reclaim spare disks via LVM remount with explicit free-space reserves, not only delete-based reclaim","technique":"Optional path-based rm -rf of large tool dirs; LVM loop images on / and /mnt concatenated into a VG, then mount over $GITHUB_WORKSPACE","url_or_slug":"easimon/maximize-build-space","what_it_cleans":"Optional preinstalled SDKs (dotnet/android/haskell/codeql/docker) plus unused root and /mnt temp disk reclaimed into a large build volume (often 60+ GB)"},{"language":"JavaScript","name":"gha-remove-artifacts","safety":"age + skip-tags + skip-recent policies; Octokit rate-limit throttling; permanent delete via API (no trash/restore)","stars_approx":"391","steal":"Layer keep-last-N and keep-tagged-releases on top of pure age-based deletion for monorepo release vs PR artifact policies","technique":"GitHub REST Actions Artifacts API list/delete; age threshold parsed by Moment.js; optional keep of tagged-release and N most recent artifacts","url_or_slug":"c-hive/gha-remove-artifacts","what_it_cleans":"Repository workflow artifacts that consume GitHub Actions artifact/log storage quota (scheduled purge beyond default retention)"},{"language":"Go","name":"gh-actions-cache","safety":"Interactive confirmation before delete (--confirm to skip); list-first UX; branch-scoped delete; no local filesystem trash","stars_approx":"332","steal":"Sort candidates by size or last-used before prune so monorepos delete the largest/stale caches first (same UX as modern gh cache)","technique":"Actions Cache REST API; list with key-prefix and branch filters; sort by last-used/size/created-at; delete by cache key (multi-branch keys supported)","url_or_slug":"actions/gh-actions-cache","what_it_cleans":"GitHub Actions dependency caches for a repo (by key/branch); archived OSS core whose model lives on as native gh cache"},{"language":"TypeScript","name":"delete-artifact","safety":"Explicit name/glob allowlist of what to delete; failOnError:false so optional cleanup does not fail the job; version matrix pinned to upload-artifact generations","stars_approx":"232","steal":"Mid-pipeline ephemeral delete once an artifact is consumed by downstream jobs, instead of waiting for retention or a nightly bot","technique":"Actions artifact APIs scoped to the run; multi-name and glob patterns (e.g. artifact-*) matched against uploaded names","url_or_slug":"GeekyEggo/delete-artifact","what_it_cleans":"Artifacts within the current workflow run after they have been shared across jobs (immediate storage reclaim, not only retention expiry)"},{"language":"C","name":"rmlint","safety":"Non-interactive by design: writes inspectable shell script and JSON before any delete; caching/replay of prior scans","stars_approx":"2.4k","steal":"Emit a human-reviewable action plan (shell/JSON) as the default delete path instead of immediate unlink","technique":"Recursive walk; group by size then hash; optional mtime filter and paranoia full-compare; BTRFS-aware; multi-format report","url_or_slug":"sahib/rmlint","what_it_cleans":"Duplicate files/dirs, empty files/dirs, broken symlinks, non-stripped binaries, files with broken UID/GID"},{"language":"Rust","name":"fclones","safety":"Separate group vs remove/link phases; --dry-run; keep-path/keep-name and priority (newest/oldest); inspect report before mutation","stars_approx":"2.9k","steal":"Hard split discovery (group report) from mutation, with priority and keep-path rules applied only at act time","technique":"Multi-stage pipeline: size group, inode collapse, prefix/suffix partial hash, full content hash; device-type-tuned I/O; optional hash cache","url_or_slug":"pkolaczk/fclones","what_it_cleans":"Redundant identical files (remove, hard/soft link, or FS reflink dedupe)"},{"language":"Rust","name":"czkawka","safety":"GUI/CLI selection before delete; offline/no telemetry; multi-frontend reuse of core; user chooses targets after scan","stars_approx":"32.5k","steal":"Modular multi-scanner core library (empty/temp/similar/broken) behind one API instead of a single junk category","technique":"Walk + size/hash for dupes; specialized heuristics (perceptual image hash, audio tags/content, video frames); rescan cache; shared czkawka_core library","url_or_slug":"qarmin/czkawka","what_it_cleans":"Duplicates, empty folders/files, temp files, big files, similar images/videos/music, broken files, bad extensions, invalid symlinks, EXIF (GUI tools)"},{"language":"Shell","name":"wipe-modules","safety":"-D/--dry lists only; regenerable via package.json; no system paths; explicit path + days required","stars_approx":"358","steal":"Inactivity-by-mtime threshold for regenerable build artifacts (not just directory name presence)","technique":"Walk code tree for node_modules; mark inactive by project mtime older than user-specified days; cron-friendly agent","url_or_slug":"bntzio/wipe-modules","what_it_cleans":"node_modules directories of inactive JS projects under a code root"},{"language":"Go","name":"mac-cleanup-go","safety":"Preview-first TUI; default move to Trash; impact levels safe/moderate/risky/manual with risky unselected; CLI --dry-run; confirm before delete","stars_approx":"436","steal":"Bake impact tiers into the rules DB so risky targets start excluded and only Trash after explicit include + confirm","technique":"Curated rules DB (~100+ known paths) scanned in parallel; brew/docker builtins; mtime filter for old downloads; SIP paths skipped","url_or_slug":"2ykwang/mac-cleanup-go","what_it_cleans":"macOS caches, logs, temp, browser/app data, Homebrew/Docker caches, old downloads (not full system optimizer/uninstaller)"}]
```

## Platforms (JSON)

```json
[{"categories":[{"default_enabled":true,"id":"macos-user-caches","label":"User Library Caches","notes":"Safe regenerable app caches. Prefer contents-only delete with min_age. Exclude CloudKit, HomeKit, Find My, and Safari from bulk wipe (Safari handled separately). Do not delete the Caches directory itself.","paths_or_globs":["$HOME/Library/Caches"],"risk":"low"},{"default_enabled":true,"id":"macos-user-logs","label":"User Library Logs","notes":"User-space app logs only (~/Library/Logs). Safe to remove contents; leave the Logs folder. Do not include /var/log or /private/var/log (system).","paths_or_globs":["$HOME/Library/Logs"],"risk":"low"},{"default_enabled":true,"id":"macos-saved-state","label":"Saved Application State","notes":"Window restore / Resume state (*.savedState). Apps recreate on next launch. Only clears restore data, not documents.","paths_or_globs":["$HOME/Library/Saved Application State"],"risk":"low"},{"default_enabled":true,"id":"macos-user-tmpdir","label":"User temporary directory (TMPDIR)","notes":"Resolves to this user's /private/var/folders/.../T/. Clean aged contents only; skip open/in-use files. Never wipe sibling user folders under /private/var/folders.","paths_or_globs":["$TMPDIR"],"risk":"low"},{"default_enabled":false,"id":"macos-system-tmp","label":"Shared system temp (/tmp)","notes":"World-writable shared temps. Default off: other users and running processes may use files here. Age-filter + contents-only if enabled; never delete the directory nodes.","paths_or_globs":["/tmp","/private/tmp"],"risk":"medium"},{"default_enabled":false,"id":"macos-var-folders-report","label":"Darwin /private/var/folders (report-only)","notes":"Per-user 0/C/T trees for all accounts. Report size only; never auto-delete the tree. User cleanup goes through $TMPDIR (T) or careful owned C caches, not a bulk rm of /private/var/folders.","paths_or_globs":["/private/var/folders"],"risk":"report-only"},{"default_enabled":true,"id":"macos-homebrew-cache","label":"Homebrew download cache","notes":"Default brew --cache is $HOME/Library/Caches/Homebrew (overridable via HOMEBREW_CACHE). Cached bottles/sources; brew re-downloads as needed. Prefer brew cleanup when available.","paths_or_globs":["$HOME/Library/Caches/Homebrew","$HOMEBREW_CACHE"],"risk":"low"},{"default_enabled":false,"id":"macos-xcode-derived-data","label":"Xcode DerivedData","notes":"Default DerivedData location (Xcode Settings > Locations). Safe but forces full rebuilds/reindex. Default off for developer machines.","paths_or_globs":["$HOME/Library/Developer/Xcode/DerivedData"],"risk":"medium"},{"default_enabled":false,"id":"macos-ios-device-support","label":"Xcode device support symbols","notes":"Symbols for attached devices; large and regenerable when the device reconnects. Prefer deleting old OS version folders only.","paths_or_globs":["$HOME/Library/Developer/Xcode/iOS DeviceSupport","$HOME/Library/Developer/Xcode/watchOS DeviceSupport","$HOME/Library/Developer/Xcode/tvOS DeviceSupport","$HOME/Library/Developer/Xcode/visionOS DeviceSupport"],"risk":"medium"},{"default_enabled":false,"id":"macos-xcode-archives","label":"Xcode Archives (report-only)","notes":"May be required for App Store uploads, TestFlight, and crash symbolication (dSYM). Measure only; delete only via Organizer with user intent.","paths_or_globs":["$HOME/Library/Developer/Xcode/Archives"],"risk":"report-only"},{"default_enabled":false,"id":"macos-coresimulator-caches","label":"CoreSimulator caches","notes":"Simulator dyld/runtime caches only. Do not delete Devices or system runtimes under /Library/Developer/CoreSimulator without explicit user action (use simctl for devices/runtimes).","paths_or_globs":["$HOME/Library/Developer/CoreSimulator/Caches"],"risk":"medium"},{"default_enabled":false,"id":"macos-mail-downloads","label":"Mail Downloads (attachments)","notes":"Attachments Mail opened and copied here; not mailbox store. Can free space but may remove local copies still wanted. Never touch $HOME/Library/Mail or other Mail container data (Accounts, MailData, Preferences).","paths_or_globs":["$HOME/Library/Containers/com.apple.mail/Data/Library/Mail Downloads"],"risk":"medium"},{"default_enabled":false,"id":"macos-browser-caches","label":"Browser disk caches","notes":"Disk/network caches only; do not touch Application Support profiles, cookies, logins, or ~/Library/Safari bookmarks/history. Close browsers first when possible. Safari/WebKit caches can be recreated but are stickier while Safari is running.","paths_or_globs":["$HOME/Library/Caches/Google/Chrome","$HOME/Library/Caches/Chromium","$HOME/Library/Caches/Microsoft Edge","$HOME/Library/Caches/com.microsoft.edgemac","$HOME/Library/Caches/Firefox","$HOME/Library/Caches/BraveSoftware","$HOME/Library/Caches/com.apple.Safari","$HOME/Library/Caches/com.apple.Safari.SafeBrowsing"],"risk":"medium"},{"default_enabled":true,"id":"macos-quicklook-cache","label":"Quick Look thumbnail cache","notes":"Thumbnail caches rebuilt by Quick Look as files are previewed.","paths_or_globs":["$HOME/Library/Caches/com.apple.QuickLook.thumbnailcache","$HOME/Library/Caches/CloudKit/com.apple.QuickLook.thumbnailcache"],"risk":"low"},{"default_enabled":false,"id":"macos-swiftpm-cache","label":"Swift Package Manager cache","notes":"Resolved SPM package cache; re-downloaded on next build. Default off to avoid surprise network/build cost on CI-like machines.","paths_or_globs":["$HOME/Library/Caches/org.swift.swiftpm","$HOME/Library/Caches/swiftpm"],"risk":"low"}],"never_touch":["$HOME","$HOME/Documents","$HOME/Desktop","$HOME/Downloads","$HOME/Pictures","$HOME/Movies","$HOME/Music","$HOME/Photos Library.photoslibrary","$HOME/.ssh","$HOME/.gnupg","$HOME/.aws","$HOME/Library/Keychains","$HOME/Library/Mail","$HOME/Library/Messages","$HOME/Library/Mobile Documents","$HOME/Library/CloudStorage","$HOME/Library/Preferences","$HOME/Library/Developer/Xcode/UserData","/System","/Library"],"platform":"macos"},{"categories":[{"default_enabled":true,"id":"macos-user-caches","label":"User Library Caches","notes":"Primary safe reclaim surface. Delete contents of aged app cache dirs, not the Caches folder node itself. Exclude CloudKit, HomeKit, Find My, and full Safari tree from bulk auto-clean unless browser-cache category is on. Never scan /System/Library/Caches or /Library/Caches as rule roots.","paths_or_globs":["$HOME/Library/Caches"],"risk":"low"},{"default_enabled":true,"id":"macos-user-logs","label":"User Library Logs","notes":"User-scoped app logs only. Regenerable diagnostic text; keep min_age (e.g. 7d). Do not touch /var/log or /Library/Logs without elevated opt-in.","paths_or_globs":["$HOME/Library/Logs"],"risk":"low"},{"default_enabled":true,"id":"macos-saved-application-state","label":"Saved Application State","notes":"Window restoration blobs; apps recreate on next launch. Low user impact.","paths_or_globs":["$HOME/Library/Saved Application State"],"risk":"low"},{"default_enabled":true,"id":"macos-user-tmp","label":"User temporary directory","notes":"macOS per-user temp is typically under /var/folders/.../T via $TMPDIR. Only delete aged contents; never target /private/var/folders wholesale or other users' folders.","paths_or_globs":["$TMPDIR"],"risk":"low"},{"default_enabled":true,"id":"macos-homebrew-cache","label":"Homebrew download cache","notes":"Default brew --cache location. Bottles/sources re-download; prefer brew cleanup for live Cellar interaction. Safe contents wipe of aged downloads.","paths_or_globs":["$HOME/Library/Caches/Homebrew"],"risk":"low"},{"default_enabled":true,"id":"macos-quicklook-cache","label":"Quick Look thumbnail cache","notes":"Thumbnail indexes rebuild via qlmanage/Finder. Narrow paths only; do not wipe all CloudKit.","paths_or_globs":["$HOME/Library/Caches/com.apple.QuickLook.thumbnailcache","$HOME/Library/Caches/CloudKit/com.apple.QuickLook.thumbnailcache"],"risk":"low"},{"default_enabled":false,"id":"macos-trash","label":"User Trash","notes":"User-intended discard but irreversible after empty. Off by default; confirm UI/dry-run. Volume Trashes under /.Trashes are out of scope for auto-clean.","paths_or_globs":["$HOME/.Trash"],"risk":"medium"},{"default_enabled":false,"id":"macos-xcode-derived-data","label":"Xcode DerivedData","notes":"Build intermediates; next build is cold/full. Safe to clear when Xcode is quit. Not source code.","paths_or_globs":["$HOME/Library/Developer/Xcode/DerivedData","$HOME/Library/Caches/com.apple.dt.Xcode"],"risk":"medium"},{"default_enabled":false,"id":"macos-xcode-device-support","label":"Xcode iOS DeviceSupport","notes":"Device symbols for debugging; large and regenerable when device reconnects. Keep recent platforms if actively debugging.","paths_or_globs":["$HOME/Library/Developer/Xcode/iOS DeviceSupport"],"risk":"medium"},{"default_enabled":false,"id":"macos-coresimulator-caches","label":"CoreSimulator caches","notes":"dyld and related simulator caches grow large and rebuild. Do NOT auto-delete Devices/ (wipes simulator VMs and app data); that stays report-only or manual simctl.","paths_or_globs":["$HOME/Library/Developer/CoreSimulator/Caches"],"risk":"medium"},{"default_enabled":false,"id":"macos-xcode-archives","label":"Xcode Archives","notes":"May hold dSYMs and App Store submission artifacts. Measure and list only; never auto-delete.","paths_or_globs":["$HOME/Library/Developer/Xcode/Archives"],"risk":"report-only"},{"default_enabled":false,"id":"macos-browser-disk-cache","label":"Browser disk cache (not profiles)","notes":"HTTP/disk caches only. Leaves cookies, passwords, extensions, Local Storage, and profile DBs alone. Default off because active browsers lock files and Safari cache layout varies by version.","paths_or_globs":["$HOME/Library/Caches/Google/Chrome","$HOME/Library/Caches/Chromium","$HOME/Library/Caches/Microsoft Edge","$HOME/Library/Caches/Firefox","$HOME/Library/Caches/com.apple.Safari/WebKitCache","$HOME/Library/Caches/com.apple.Safari/fsCachedData","$HOME/Library/Containers/com.apple.Safari/Data/Library/Caches"],"risk":"medium"},{"default_enabled":false,"id":"macos-browser-privacy-data","label":"Browser profiles and privacy data","notes":"EXPLICIT PRIVACY MODE ONLY. Logs users out, drops history/cookies/site data. Blocked by never_touch for normal auto-clean; enable only with dual confirmation and dry-run. Prefer browser-native clear APIs when available.","paths_or_globs":["$HOME/Library/Application Support/Google/Chrome","$HOME/Library/Application Support/Firefox","$HOME/Library/Application Support/Microsoft Edge","$HOME/Library/Safari","$HOME/Library/Cookies","$HOME/Library/Caches/com.apple.Safari"],"risk":"high"},{"default_enabled":false,"id":"macos-mail-downloads","label":"Mail attachment downloads","notes":"Attachments Mail saved to disk; may be the only local copy. Requires narrow allowlist of this subpath only; never delete $HOME/Library/Mail or the whole com.apple.mail container.","paths_or_globs":["$HOME/Library/Containers/com.apple.mail/Data/Library/Mail Downloads"],"risk":"medium"},{"default_enabled":false,"id":"macos-ios-device-backups","label":"Local iPhone/iPad backups","notes":"Full device backups via Finder/iTunes. Irreplaceable without another backup. Report size only; user deletes via Finder Manage Backups or explicit manual action.","paths_or_globs":["$HOME/Library/Application Support/MobileSync/Backup"],"risk":"report-only"},{"default_enabled":true,"id":"macos-package-manager-caches","label":"Language package manager caches","notes":"Regenerable download caches for common toolchains on macOS. Do not delete pnpm global store, full .cargo registry sources blindly, or project node_modules/target dirs from this category.","paths_or_globs":["$HOME/Library/Caches/pip","$HOME/Library/Caches/Yarn","$HOME/Library/Caches/npm","$HOME/Library/Caches/go-build","$HOME/Library/Caches/CocoaPods","$HOME/.npm/_cacache","$HOME/.cache/pip","$HOME/.cache/yarn","$XDG_CACHE_HOME/pip","$XDG_CACHE_HOME/yarn"],"risk":"low"}],"never_touch":["/System","/Applications","/Library","/usr","/bin","/sbin","/private/var/db","$HOME/Documents","$HOME/Desktop","$HOME/Pictures","$HOME/Photos Library.photoslibrary","$HOME/.ssh","$HOME/.gnupg","$HOME/Library/Keychains","$HOME/Library/Mail","$HOME/Library/Messages","$HOME/Library/Mobile Documents","$HOME/Library/CloudStorage","$HOME/Library/Application Support/Google/Chrome","$HOME/Library/Safari"],"platform":"macos"},{"categories":[{"default_enabled":true,"id":"xdg-user-cache","label":"XDG user cache","notes":"Non-essential app caches per XDG (default $HOME/.cache). Prefer contents-only delete with min_age_days>=3. Exclude GPU/driver shader dirs (mesa_shader_cache, nvidia, amd/gl_shader_cache) if wipe causes long recompiles. Do not treat as a free-for-all over nested browser profile data if linked outside pure cache trees.","paths_or_globs":["$XDG_CACHE_HOME","$HOME/.cache"],"risk":"low"},{"default_enabled":true,"id":"thumbnails","label":"Thumbnail cache","notes":"FreeDesktop thumbnail cache (normal/large/x-large/xx-large/fail). Regenerated by file managers. Legacy ~/.thumbnails still appears on older desktops. Safe contents wipe; min_age_days>=7 optional to keep recent previews.","paths_or_globs":["$XDG_CACHE_HOME/thumbnails","$HOME/.cache/thumbnails","$HOME/.thumbnails"],"risk":"low"},{"default_enabled":true,"id":"user-temp-and-trash","label":"User temp and Trash","notes":"Age-filter $TMPDIR (min_age_days>=2). Trash is user-recoverable junk. Never wipe whole $XDG_RUNTIME_DIR (sockets/locks for live session); if included, only age-stale nested cache-like subdirs or omit runtime entirely and keep Trash+$TMPDIR. Prefer omitting $XDG_RUNTIME_DIR from auto-clean in implementation.","paths_or_globs":["$TMPDIR","$XDG_RUNTIME_DIR","$HOME/.local/share/Trash/files","$HOME/.local/share/Trash/info"],"risk":"low"},{"default_enabled":true,"id":"user-logs","label":"User log files","notes":"Restrict to **/*.log and **/*.old under state trees. XDG_STATE_HOME defaults to $HOME/.local/state. Do not delete non-log state (history DBs, app state). min_age_days>=14.","paths_or_globs":["$HOME/.local/state","$HOME/.local/share/xorg","$XDG_STATE_HOME"],"risk":"low"},{"default_enabled":false,"id":"journald","label":"systemd journal (journald)","notes":"Persistent journals live under /var/log/journal/<machine-id>/; volatile under /run/log/journal. Never unlink active *.journal files from disk directly. Size control must be journalctl --vacuum-size/--vacuum-time (optionally after --rotate). Report size only; cleaning requires a dedicated privileged vacuum action, not generic file delete.","paths_or_globs":["/var/log/journal","/run/log/journal"],"risk":"report-only"},{"default_enabled":false,"id":"apt-package-cache","label":"APT package cache (Debian/Ubuntu)","notes":"Downloaded .deb payloads; equivalent to apt clean. Regenerable; needs root. Keep lock files; do not delete /var/lib/apt/lists (package indexes) or /var/lib/dpkg. default_enabled false because system/root scope.","paths_or_globs":["/var/cache/apt/archives/*.deb","/var/cache/apt/archives/partial/*","/var/cache/apt/archives"],"risk":"low"},{"default_enabled":false,"id":"pacman-package-cache","label":"pacman package cache (Arch family)","notes":"Default CacheDir is /var/cache/pacman/pkg/. Safe to remove packages but loses easy downgrade/reinstall offline. Prefer paccache -rkN semantics over wiping every package. Needs root. Never touch /var/lib/pacman.","paths_or_globs":["/var/cache/pacman/pkg","/var/cache/pacman/pkg/*"],"risk":"medium"},{"default_enabled":false,"id":"dnf-package-cache","label":"DNF package cache (Fedora/RHEL)","notes":"dnf4 uses /var/cache/dnf; Fedora 41+ dnf5 also uses /var/cache/libdnf5. Metadata and any keepcache RPMs are regenerable (dnf clean all). Needs root. Never delete /var/lib/dnf or RPM DB under /var/lib/rpm.","paths_or_globs":["/var/cache/dnf","/var/cache/libdnf5","/var/tmp/dnf-*"],"risk":"low"},{"default_enabled":false,"id":"systemd-tmpfiles-temps","label":"systemd-tmpfiles temporary dirs","notes":"systemd-tmpfiles ages /tmp (~10d) and /var/tmp (~30d) via tmpfiles.d. Auto-clean only aged file contents, never the directory roots, never sockets currently in use, never /tmp/.X11-unix, /tmp/.ICE-unix, systemd private dirs. Never delete tmpfiles configs under /etc/tmpfiles.d, /usr/lib/tmpfiles.d, /run/tmpfiles.d, or $HOME/.config/user-tmpfiles.d. Prefer invoking systemd-tmpfiles --clean when possible.","paths_or_globs":["/tmp","/var/tmp"],"risk":"medium"},{"default_enabled":true,"id":"flatpak-app-cache","label":"Flatpak per-app caches","notes":"Sandbox XDG cache for each Flatpak app under ~/.var/app/<app-id>/cache. Regenerable. Do not delete sibling config or data dirs. min_age_days>=3.","paths_or_globs":["$HOME/.var/app/*/cache","$HOME/.var/app/*/cache/**"],"risk":"low"},{"default_enabled":false,"id":"flatpak-leftovers","label":"Flatpak installs and unused runtimes","notes":"Installed apps/runtimes live in user ~/.local/share/flatpak and system /var/lib/flatpak; leftover app data after uninstall stays in ~/.var/app/<id>. Blind rm breaks installs and OSTree repos. Report sizes only; cleanup via flatpak uninstall --unused [--delete-data] and flatpak repair, not file-level delete of repo/objects.","paths_or_globs":["$HOME/.local/share/flatpak","/var/lib/flatpak","$HOME/.var/app"],"risk":"report-only"},{"default_enabled":false,"id":"snap-download-cache","label":"Snap download cache","notes":"snapd working/download cache; safe to clear (may force re-download). Needs root. Distinct from mounted/active snap images in /var/lib/snapd/snaps.","paths_or_globs":["/var/lib/snapd/cache","/var/lib/snapd/cache/*"],"risk":"low"},{"default_enabled":false,"id":"snap-leftovers","label":"Snap revisions and user leftovers","notes":"Old revisions under /var/lib/snapd/snaps and ~/snap/<name>/<rev> must be pruned with snap remove --revision or snap set system refresh.retain=N, not rm. User data in ~/snap and /var/snap can hold important app state. Report only; optional future medium rule limited to $HOME/snap/*/*/.cache if clearly cache.","paths_or_globs":["/var/lib/snapd/snaps","/var/snap","$HOME/snap","$HOME/snap/*/*/.cache"],"risk":"report-only"},{"default_enabled":false,"id":"old-kernels","label":"Old kernels and modules","notes":"Always report-only. Removing the running or only fallback kernel bricks the system. Instruct users to purge via apt autoremove / dnf remove $(dnf repoquery --installonly --latest-limit=-2 -q) / package-manager tools. Never auto-delete /boot or module trees.","paths_or_globs":["/boot/vmlinuz-*","/boot/initrd.img-*","/boot/initramfs-*.img","/boot/System.map-*","/boot/config-*","/lib/modules/*","/usr/lib/modules/*"],"risk":"report-only"}],"never_touch":["/","$HOME","$HOME/Documents","$HOME/Desktop","$HOME/Downloads","$HOME/Pictures","$HOME/Videos","$HOME/Music","$HOME/.ssh","$HOME/.gnupg","$HOME/.config","$XDG_CONFIG_HOME","/boot","/etc","/usr","/bin","/sbin","/lib","/lib64","/var/lib/dpkg"],"platform":"linux"},{"categories":[{"default_enabled":true,"id":"windows-temp-user","label":"User TEMP","notes":"Default user temp is %LOCALAPPDATA%\\Temp (via %TEMP%/%TMP%). Delete contents only; keep the folder; skip in-use files; min_age_days >= 1–2 recommended.","paths_or_globs":["$TEMP","$TMP","%LOCALAPPDATA%/Temp"],"risk":"low"},{"default_enabled":false,"id":"windows-temp-system","label":"System TEMP","notes":"C:\\Windows\\Temp (or %WINDIR%\\Temp). Often needs elevation; many files locked by services. Contents-only; never delete the Temp directory itself.","paths_or_globs":["%WINDIR%/Temp"],"risk":"medium"},{"default_enabled":false,"id":"windows-prefetch","label":"Prefetch","notes":"Default %WINDIR%\\Prefetch (*.pf, ReadyBoot). Safe-ish contents clear but slows cold starts until rebuilt and erases launch traces. Prefer report or opt-in; do not wipe Layout.ini aggressively; avoid if Superfetch/SysMain analysis matters.","paths_or_globs":["%WINDIR%/Prefetch"],"risk":"medium"},{"default_enabled":false,"id":"windows-winsxs","label":"WinSxS component store","notes":"Report size only. Never auto-delete. Cleanup only via DISM StartComponentCleanup / Storage Sense / Disk Cleanup. Manual deletes break servicing.","paths_or_globs":["%WINDIR%/WinSxS"],"risk":"report-only"},{"default_enabled":false,"id":"windows-delivery-optimization","label":"Delivery Optimization cache","notes":"Default cache drive is %SystemDrive% (DOModifyCacheDrive). Live cache usually under NetworkService DeliveryOptimization\\Cache; alternate layouts exist. Prefer Settings/Disk Cleanup; stop DoSvc if clearing files; admin required; cache rebuilds on next updates.","paths_or_globs":["%WINDIR%/ServiceProfiles/NetworkService/AppData/Local/Microsoft/Windows/DeliveryOptimization/Cache","%WINDIR%/DeliveryOptimization/Cache","%SystemDrive%/ProgramData/Microsoft/Windows/DeliveryOptimization/Cache"],"risk":"medium"},{"default_enabled":false,"id":"windows-browser-cache","label":"Browser disk caches","notes":"Cache-only under Chromium profiles and Firefox cache2/startupCache; INetCache is legacy web cache. Never touch Cookies, Login Data, Web Data, Local Storage, IndexedDB, Preferences, or whole User Data/profile roots. Close browsers first.","paths_or_globs":["%LOCALAPPDATA%/Google/Chrome/User Data/*/Cache","%LOCALAPPDATA%/Google/Chrome/User Data/*/Code Cache","%LOCALAPPDATA%/Google/Chrome/User Data/*/GPUCache","%LOCALAPPDATA%/Microsoft/Edge/User Data/*/Cache","%LOCALAPPDATA%/Microsoft/Edge/User Data/*/Code Cache","%LOCALAPPDATA%/Microsoft/Edge/User Data/*/GPUCache","%LOCALAPPDATA%/Mozilla/Firefox/Profiles/*/cache2","%LOCALAPPDATA%/Mozilla/Firefox/Profiles/*/startupCache","%LOCALAPPDATA%/Microsoft/Windows/INetCache"],"risk":"medium"},{"default_enabled":false,"id":"windows-old","label":"Windows.old","notes":"Previous Windows installation after in-place upgrade. Report only; remove via Settings > System > Storage or Disk Cleanup (Previous Windows installation). Direct delete may fail ACLs and blocks rollback.","paths_or_globs":["%SystemDrive%/Windows.old"],"risk":"report-only"},{"default_enabled":false,"id":"windows-recycle-bin","label":"Recycle Bin","notes":"Per-volume $Recycle.Bin holds recoverable user files. Opt-in only. Path expander must treat $Recycle.Bin as a literal name (not a $variable). Prefer SHEmptyRecycleBin API over raw deletes when possible.","paths_or_globs":["%SystemDrive%/$Recycle.Bin","C:/$Recycle.Bin","D:/$Recycle.Bin"],"risk":"medium"},{"default_enabled":true,"id":"windows-thumbnail-cache","label":"Thumbnail and icon cache","notes":"thumbcache_*.db under %LOCALAPPDATA%\\Microsoft\\Windows\\Explorer; legacy Thumbs.db in folders. Safe to delete; Explorer rebuilds. May need Explorer restart if files are locked. Do not wipe entire Explorer folder (other settings live there).","paths_or_globs":["%LOCALAPPDATA%/Microsoft/Windows/Explorer/thumbcache_*.db","%LOCALAPPDATA%/Microsoft/Windows/Explorer/iconcache_*.db","%LOCALAPPDATA%/IconCache.db","%USERPROFILE%/**/Thumbs.db"],"risk":"low"}],"never_touch":["%USERPROFILE%","%USERPROFILE%/Documents","%USERPROFILE%/Desktop","%USERPROFILE%/Downloads","%USERPROFILE%/Pictures","%USERPROFILE%/Videos","%USERPROFILE%/Music","%USERPROFILE%/.ssh","%USERPROFILE%/.gnupg","%WINDIR%/System32","%WINDIR%/SysWOW64","%WINDIR%/WinSxS","%WINDIR%/Boot","C:/Program Files","C:/Program Files (x86)","%SystemDrive%/pagefile.sys","%SystemDrive%/hiberfil.sys","%SystemDrive%/swapfile.sys","%LOCALAPPDATA%/**/User Data/*/Cookies","%LOCALAPPDATA%/**/User Data/*/Login Data"],"platform":"windows"},{"categories":[{"default_enabled":false,"id":"js-node-modules","label":"Node.js project node_modules","notes":"Regenerable via npm/yarn/pnpm install. Never full-home auto-delete: only under explicit project scan roots, require package.json/lockfile sibling, min_age_days>=14, max_depth bound. Excludes Yarn PnP .yarn/cache (project policy) and pnpm content store.","paths_or_globs":["**/node_modules","**/node_modules/**"],"risk":"medium"},{"default_enabled":false,"id":"rust-target","label":"Rust Cargo target/ build dirs","notes":"Default is workspace-root target/ (or CARGO_TARGET_DIR). Match only when Cargo.toml exists at parent/workspace root. Forces full recompile. Do not delete $HOME/.cargo/bin or source crates.","paths_or_globs":["**/target","**/target/debug","**/target/release","**/target/incremental"],"risk":"medium"},{"default_enabled":false,"id":"web-build-artifacts","label":"Web/frontend build outputs (dist, .next, out)","notes":"Common regenerable frontend outputs. Avoid bare **/build (collides with CMake/Python/Make). Require project markers (package.json, etc.) and min_age. May contain deliberately kept release artifacts: keep opt-in.","paths_or_globs":["**/dist","**/out","**/.next","**/.nuxt","**/.turbo","**/.vite","**/storybook-static"],"risk":"medium"},{"default_enabled":true,"id":"python-bytecode","label":"Python __pycache__ and .pyc","notes":"Bytecode is always regenerable. Default-enabled only under known cache trees (existing dev-pycache-home pattern). Project-tree **/__pycache__ should stay opt-in or age-gated to avoid walking all of $HOME.","paths_or_globs":["$HOME/.cache","$XDG_CACHE_HOME","%LOCALAPPDATA%","**/__pycache__","**/*.pyc","**/*.pyo"],"risk":"low"},{"default_enabled":true,"id":"js-package-caches","label":"npm and Yarn download caches","notes":"Official npm cache: ~/.npm (posix) or %LocalAppData%/npm-cache (Windows). Yarn classic/berry user cache under XDG or Library/Caches. Prefer contents delete + min_age_days>=14. Not node_modules, not pnpm store.","paths_or_globs":["$HOME/.npm/_cacache","$HOME/.npm/_logs","%LOCALAPPDATA%/npm-cache","$HOME/.cache/yarn","$XDG_CACHE_HOME/yarn","$HOME/Library/Caches/Yarn","$HOME/Library/Caches/npm"],"risk":"low"},{"default_enabled":true,"id":"python-package-caches","label":"pip and Python package caches","notes":"HTTP wheel/sdist cache only. Do not touch virtualenvs (.venv, venv) or conda envs.","paths_or_globs":["$HOME/.cache/pip","$XDG_CACHE_HOME/pip","$HOME/Library/Caches/pip","%LOCALAPPDATA%/pip/Cache"],"risk":"low"},{"default_enabled":false,"id":"gradle-caches","label":"Gradle dependency and build caches","notes":"Gradle User Home caches (~/.gradle/caches). Safe to wipe but forces large re-downloads. Never delete project source or required wrapper scripts outside dists. Project-level .gradle/ is separate and higher risk.","paths_or_globs":["$HOME/.gradle/caches","$HOME/.gradle/.tmp","$HOME/.gradle/daemon","$HOME/.gradle/native","$HOME/.gradle/wrapper/dists"],"risk":"medium"},{"default_enabled":false,"id":"xcode-derived-data","label":"Xcode DerivedData and iOS DeviceSupport","notes":"DerivedData default remains ~/Library/Developer/Xcode/DerivedData. Regenerable; full rebuilds. Archives (~/Library/Developer/Xcode/Archives) stay report-only (dSYM/App Store).","paths_or_globs":["$HOME/Library/Developer/Xcode/DerivedData","$HOME/Library/Developer/Xcode/iOS DeviceSupport","$HOME/Library/Developer/Xcode/watchOS DeviceSupport","$HOME/Library/Developer/Xcode/tvOS DeviceSupport","$HOME/Library/Developer/CoreSimulator/Caches"],"risk":"medium"},{"default_enabled":false,"id":"android-sdk-leftovers","label":"Android SDK caches and unused system-images","notes":"SDK defaults: macOS ~/Library/Android/sdk, Windows %LOCALAPPDATA%\\Android\\Sdk, Linux ~/Android/Sdk or ANDROID_HOME. Only caches/temp/system-images leftovers—never whole SDK, platform-tools, or licenses. AVD userdata is never_touch.","paths_or_globs":["$HOME/Library/Android/sdk/.temp","$HOME/Library/Android/sdk/system-images","$HOME/Library/Android/sdk/build-tools","%LOCALAPPDATA%/Android/Sdk/.temp","%LOCALAPPDATA%/Android/Sdk/system-images","$HOME/Android/Sdk/.temp","$HOME/Android/Sdk/system-images","$HOME/.android/cache","$HOME/.android/build-cache","$HOME/.android/metrics"],"risk":"medium"},{"default_enabled":false,"id":"docker-disk-report","label":"Docker Desktop images, volumes, and VM disk","notes":"macOS Docker.raw under Containers/.../vms/0/data; Windows docker_data.vhdx under Local\\Docker\\wsl (path varies by Desktop version). File-delete destroys all images/volumes. Report size only; reclaim via docker system prune / Desktop.","paths_or_globs":["$HOME/Library/Containers/com.docker.docker/Data/vms","$HOME/Library/Group Containers/group.com.docker","%LOCALAPPDATA%/Docker/wsl","%LOCALAPPDATA%/Docker/wsl/disk/docker_data.vhdx","$HOME/.docker/buildx","/var/lib/docker"],"risk":"report-only"},{"default_enabled":true,"id":"asdf-mise-downloads","label":"asdf/mise download and install caches","notes":"Download/cache only (asdf downloads/; mise cache dir). Does not remove installed tool versions. Prefer contents delete + min_age_days>=7.","paths_or_globs":["$HOME/.asdf/downloads","$HOME/.asdf/tmp","$HOME/.cache/mise","$XDG_CACHE_HOME/mise","$HOME/Library/Caches/mise","$HOME/.local/share/mise/downloads","$XDG_DATA_HOME/mise/downloads"],"risk":"low"},{"default_enabled":false,"id":"asdf-mise-installs","label":"asdf/mise installed tool versions (unused)","notes":"asdf: ${ASDF_DATA_DIR:-$HOME/.asdf}/installs/<plugin>/<version>. mise: ~/.local/share/mise/installs (XDG). Never bulk-delete: only report, or delete versions proven unused vs .tool-versions/mise.toml/global config. Keep shims/plugins/config.","paths_or_globs":["$HOME/.asdf/installs","$HOME/.local/share/mise/installs","$XDG_DATA_HOME/mise/installs","$HOME/.local/share/mise/installs/**","$HOME/.asdf/installs/**"],"risk":"report-only"},{"default_enabled":false,"id":"pnpm-store","label":"pnpm content-addressable store","notes":"Shared store; deleting breaks projects until re-fetch. Prefer pnpm store prune for unused packages. Report size only unless user explicitly overrides.","paths_or_globs":["$HOME/.local/share/pnpm/store","$XDG_DATA_HOME/pnpm/store","$HOME/Library/pnpm/store","%LOCALAPPDATA%/pnpm/store","$HOME/.pnpm-store"],"risk":"report-only"},{"default_enabled":false,"id":"cargo-registry-cache","label":"Cargo registry and git dependency cache","notes":"Downloaded crate sources/cache only—not project target/. Never delete $HOME/.cargo/bin (installed binaries) or .cargo/config.toml.","paths_or_globs":["$HOME/.cargo/registry/cache","$HOME/.cargo/registry/src","$HOME/.cargo/git/db","$HOME/.cargo/git/checkouts"],"risk":"medium"}],"never_touch":["$HOME","$HOME/.ssh","$HOME/.gnupg","$HOME/.aws","$HOME/.config/gcloud","$HOME/.docker/config.json","$HOME/.docker/contexts","$HOME/.cargo/bin","$HOME/.asdf/shims","$HOME/.asdf/plugins","$HOME/.local/share/mise/shims","$HOME/.android/avd","$HOME/.gradle/wrapper/dists/**/gradle-*/bin","**/.git","$HOME/Documents","$HOME/Desktop","$HOME/Library/Keychains","/var/lib/docker","%USERPROFILE%\\Documents","C:\\Windows"],"platform":"any"},{"categories":[{"default_enabled":false,"id":"privacy-recent-docs","label":"macOS recent documents and apps","notes":"Opt-in privacy wipe of Apple menu / Finder recents. Confirmed .sfl4 on current macOS; globs cover older .sfl2/.sfl3. Does not include FavoriteItems/FavoriteVolumes/ProjectsItems. Regenerates as files are opened.","paths_or_globs":["$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.RecentDocuments.sfl*","$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.RecentApplications.sfl*","$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.RecentServers.sfl*","$HOME/Library/Preferences/com.apple.recentitems.plist"],"risk":"medium"},{"default_enabled":false,"id":"privacy-app-recents","label":"Per-app Open Recent and Office recents","notes":"ApplicationRecentDocuments holds per-app .sfl* Open Recent lists. Office securebookmarks.plist files hold Word/Excel/PowerPoint recent lists on Mac. Close Office first. Cloud-synced Microsoft account recents may reappear after sign-in.","paths_or_globs":["$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.ApplicationRecentDocuments","$HOME/Library/Containers/com.microsoft.Word/Data/Library/Preferences/com.microsoft.Word.securebookmarks.plist","$HOME/Library/Containers/com.microsoft.Excel/Data/Library/Preferences/com.microsoft.Excel.securebookmarks.plist","$HOME/Library/Containers/com.microsoft.Powerpoint/Data/Library/Preferences/com.microsoft.Powerpoint.securebookmarks.plist"],"risk":"medium"},{"default_enabled":false,"id":"privacy-clipboard","label":"Clipboard history stores (third-party)","notes":"No durable system clipboard history path on macOS comparable to Windows Clipboard folder; targets common managers (Maccy, Paste, Alfred). May hold passwords and tokens. Quit the manager before delete. Prefer contents of store dirs, not unrelated container siblings.","paths_or_globs":["$HOME/Library/Containers/org.p0deje.Maccy","$HOME/Library/Containers/com.wiheads.paste/Data/Library/Application Support/Paste","$HOME/Library/Application Support/Alfred/Databases/clipboard.alfdb","$HOME/Library/Application Support/Alfred/Databases/clipboard.alfdb.data"],"risk":"high"},{"default_enabled":false,"id":"privacy-shell-history","label":"Shell and REPL history","notes":"NEVER default-enable. Histories can contain secrets, tokens, and private paths. Does not touch shell config (.zshrc, .bashrc, fish config). New empty history files may be recreated on next session.","paths_or_globs":["$HOME/.zsh_history","$HOME/.bash_history","$HOME/.local/share/fish/fish_history","$HOME/.python_history","$HOME/.node_repl_history","$HOME/.mysql_history","$HOME/.psql_history","$HOME/.sqlite_history","$HOME/.lesshst","$HOME/.viminfo"],"risk":"high"},{"default_enabled":false,"id":"privacy-browser-cookies","label":"Browser cookies (opt-in)","notes":"Opt-in only. Logs out websites. Safari primary store is the sandboxed Cookies.binarycookies path; legacy Library/Cookies may still exist. Chrome/Firefox profile DBs only; never Login Data, Web Data, or full profile dirs. Close browsers first.","paths_or_globs":["$HOME/Library/Containers/com.apple.Safari/Data/Library/Cookies","$HOME/Library/Cookies","$HOME/Library/Application Support/Google/Chrome/Default/Cookies","$HOME/Library/Application Support/Google/Chrome/Default/Cookies-journal","$HOME/Library/Application Support/Google/Chrome/Profile */Cookies","$HOME/Library/Application Support/Google/Chrome/Profile */Cookies-journal","$HOME/Library/Application Support/Firefox/Profiles/*/cookies.sqlite","$HOME/Library/Application Support/Firefox/Profiles/*/cookies.sqlite-*"],"risk":"high"},{"default_enabled":false,"id":"privacy-browser-localstorage","label":"Browser localStorage and site storage (opt-in)","notes":"Opt-in only. Clears origin-scoped site data (sessions, offline apps). Safari WebsiteData/Default also holds IndexedDB and service workers under origin subdirs. Keep separate from browser disk cache rules. Close browsers first.","paths_or_globs":["$HOME/Library/Safari/LocalStorage","$HOME/Library/Containers/com.apple.Safari/Data/Library/WebKit/WebsiteData/LocalStorage","$HOME/Library/Containers/com.apple.Safari/Data/Library/WebKit/WebsiteData/Default","$HOME/Library/Application Support/Google/Chrome/Default/Local Storage","$HOME/Library/Application Support/Google/Chrome/Default/Session Storage","$HOME/Library/Application Support/Google/Chrome/Profile */Local Storage","$HOME/Library/Application Support/Google/Chrome/Profile */Session Storage","$HOME/Library/Application Support/Firefox/Profiles/*/storage/default","$HOME/Library/Application Support/Firefox/Profiles/*/webappsstore.sqlite"],"risk":"high"}],"never_touch":["$HOME/Library/Keychains","$HOME/.ssh","$HOME/.gnupg","$HOME/.zshrc","$HOME/.bashrc","$HOME/.zprofile","$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.FavoriteItems.sfl*","$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.FavoriteVolumes.sfl*","$HOME/Library/Application Support/com.apple.sharedfilelist/com.apple.LSSharedFileList.ProjectsItems.sfl*","$HOME/Library/Application Support/Google/Chrome/**/Login Data","$HOME/Library/Application Support/Google/Chrome/**/Web Data","$HOME/Library/Application Support/Google/Chrome/**/Bookmarks","$HOME/Library/Safari/Bookmarks.plist","$HOME/Library/Application Support/Firefox/Profiles/*/logins.json","$HOME/Library/Application Support/Firefox/Profiles/*/key4.db","$HOME/Library/Mail","$HOME/Library/Messages","$HOME/Library/Mobile Documents","$HOME/Documents","$HOME/Desktop"],"platform":"macos"}]
```

## Safety (JSON)

```json
[{"anti_patterns":["Permanent unlink (rm-style) as the default cleaner action","Any clean mode that deletes without a prior dry-run/preview path","Following symlinks out of rule roots into home or system trees","Letting YAML/rules override never-touch or filesystem-root protections","Authorizing deletes on non-canonical paths (../, unresolved links, mixed roots)","Including volumes, package stores, or archives in default prune without opt-in","Force-deleting open databases or live app data and reporting success","Auto-enabling high-risk, shred, or wipe modes for aggressive cleaning","Silently ignoring blocked or failed paths without error counters","Scareware health scores or dark patterns that push permanent mass deletes"],"must_have":["Dry-run default on clean (no disk mutation without explicit apply)","Explicit apply flag (--yes or equivalent) to perform deletes","Trash-first delete via OS Trash / FreeDesktop Trash / Recycle Bin","Permanent delete only with second-factor confirm (typed phrase)","Per-rule min_age_days plus optional global age override","Hard never-touch denylist enforced at scan and delete","Path canonicalize with lexical fallback and fail-closed on error","Symlink escape guard: candidate must stay under rule root after normalize","Skip/report locked or in-use files instead of force-delete","report_only support for dangerous rule categories","Delete-time safety revalidation (TOCTOU harden)","Auditable report (human + JSON) of method, freed bytes, skips, and errors"],"rules":["Dry-run is the default for any delete path: clean without an explicit apply flag only scans and reports (BleachBit preview / docker confirm pattern).","Trash/Recycle Bin is the default delete method; permanent unlink is opt-in only, never the silent default (trash-cli / FreeDesktop recoverability).","Permanent mass delete requires a second confirmation barrier (typed phrase or interactive confirm); --yes alone is insufficient.","Per-rule minimum age thresholds gate sticky regenerable junk; global override is allowed, but age 0 only disables the age gate, not other safety checks (docker until= filters).","Never-touch paths (user data roots, secrets, system trees, filesystem roots) always override rules and are re-checked at delete time.","Authorize only after path canonicalization (canonicalize if exists, else lexical normalize of . and ..); failed normalization means refuse.","Every candidate must stay under its rule root after normalization; refuse symlink escapes that leave the allowed root.","Do not force-delete open or locked files by default: skip, report, and continue (best-effort EBUSY / sharing-violation handling).","High-blast categories (named volumes, archives, package stores, OS restore points) are report-only unless a separate, warned opt-in path is used (docker volumes excluded from default system prune).","Medium/high-risk rules and shred/wipe modes are disabled by default and never auto-enabled for a max clean score.","Revalidate existence, never-touch, and root containment between scan and delete; failures are skipped with logged reasons.","Delete deepest paths first; continue on per-item errors and surface them; report method (dry-run|trash|permanent), counts, bytes, and skips with no silent blocked deletes.","Core junk cleaning must not require root/admin; privileged system cleaning is out of the default MVP path.","Secure overwrite/shred is out of MVP default backends and, if added later, still inherits dry-run, trash preference, never-touch, and confirm rules."],"topic":"Deletion safety model for a system cleaner: dry-run default, trash vs permanent delete, age thresholds, open-file checks, symlink escape prevention, path canonicalization"},{"anti_patterns":["Never ship or accept rules that wipe whole home, Documents, Desktop, or Downloads without hard blocks","Never execute shell/binaries from rule YAML/JSON in the core engine","Never default-enable cookies, logins, mail, SSH, or key material cleaning","Never prune Docker volumes or shared package stores (e.g. pnpm store) except report-only or ultra-explicit plugin flags","Never let config disable the hard denylist or quietly force-delete report_only rules","Never unbounded-walk entire home/disk as a built-in pack rule","Never auto-apply unauthenticated remote rule-pack updates into deletes","Never hide which rule matched a deleted path","Never make secure multi-pass wipe the default clean path","Never use scareware scores, fake health meters, or registry cleaning as default value"],"must_have":["YAML rule packs with id, category, risk, platform, paths, globs, min_age_days, delete_mode, default_enabled, report_only","Multi-dir load: bundled packs plus user rules directory with documented id override","Unix and Windows path template expansion ($HOME, $XDG_*, %LOCALAPPDATA%, etc.)","Hard never-touch denylist enforced on every match after normalize","Dry-run scan report mapping path/size/risk to rule id","Trash-backed clean with confirmation; report_only never deletes","Risk gates: medium/high off by default","CLI list rules, show rule, list categories, doctor (sources/packs)","pack_version and schema_version surfaced in doctor/version output","Include/exclude globs and max_depth for precise app/browser rules","User path denylist config that always excludes matches","Platform filtering so only host-matching rules activate"],"rules":["Rules are declarative data only (YAML primary, JSON same schema); no shell/process/code execution inside rule files in core.","Every rule has a stable unique id, human name/description, category, risk, platform, and explicit path templates; scan/clean must attribute every path to a rule id and pack.","Path templates expand env/XDG/Windows variables; refuse filesystem roots and bare home as rule roots; enforce post-normalize stay-under-root against symlink escape.","Risk is binding: low may default on; medium/high default off; report_only never deletes on the normal clean path even with --yes.","Age (min_age_days), include/exclude globs, max_depth, and delete_mode (prefer contents over path) are first-class match constraints.","Hard denylist in code (Documents/Desktop/Downloads, keys/mail, system trees, home, roots) always wins; user path denylist wins over rules; user allowlist cannot override hard denylist or report_only.","Per-app targets are separate rules in named packs (apps/dev/os); browser rules are cache-only by default; cookies/logins/history stay off or high-risk separate rules.","Packs are versioned (pack_version + schema_version); load order is shipped then site then user with documented id override; refuse silent misparse of unknown schema.","Special cleaners (docker prune, package managers, registry, sqlite vacuum) are plugins with the same candidate model; default docker-like volume/data stores stay report-only or explicit opt-in.","Default delete semantics are trash/restore-friendly (trash-cli/FreeDesktop style); permanent delete is explicit; shred/overwrite is out of MVP.","Dry-run/preview and path transparency are required before destructive clean; no hidden rule expansion beyond user-selected categories/ids."],"topic":"Rule engine design: YAML/JSON clean rules (BleachBit-style), allowlist/denylist, per-app plugins, versioned rule packs"},{"anti_patterns":["Default-on or surprise delete without scan/preview","Interactive y/N with no --yes/--force for scripts","Progress, color, or logs mixed into JSON stdout","Different filter meaning between scan and clean","Exit 0 when some deletes failed","GUI-only presets required for headless clean","Opaque deletes without path and rule id","Empty stdin or pipe treated as confirmation","Unbounded table of millions of paths without limit/JSON escape"],"must_have":["Subcommands: scan, clean, doctor, rules list|show","Shared filters: category, min-age, include high-risk","Table: size, path, category, risk, rule id, category summary, truncate + hint","JSON on stdout for scan, clean, doctor, rules","clean dry-run default; --yes applies; Trash default; permanent + hard confirm","CI/non-TTY: no hang; refuse clean without --yes","TTY progress on long runs; silent under JSON/non-TTY","Documented exit codes; 0 empty success; nonzero on real failure","Actionable stderr errors with next step (e.g. pass --yes)"],"rules":["Verbs only: scan (read-only), clean (mutate), doctor (self-check), rules list|show; same filters on scan and clean so scan is a true dry-run of clean.","scan never mutates; clean defaults to dry-run and only deletes with explicit --yes (BleachBit preview vs clean).","Human default is table; machines use -o json|--format json on every data-bearing command.","JSON is complete and stable (full findings, totals, skipped roots, rule ids); table may truncate via --limit; schema additive only.","Stdout holds table/JSON payload; stderr holds progress, warnings, prompts, and errors so pipes to jq stay clean.","Exit codes are public API: 0 success (including empty scan), 1 operational/partial failure, 2 usage/config error; document in --help.","Noninteractive when not a TTY, --yes/--non-interactive, or CI: no prompts, no TTY-only spinners; refuse destructive clean if confirmation missing (fail closed, never hang).","Risk-matched confirmation: Trash delete needs --yes; permanent mass delete needs a second gate (confirm phrase); never treat piped input as yes.","Progress only on interactive TTY stderr; quiet under --json/non-TTY; SIGINT aborts with partial report and non-success if work incomplete.","rules and doctor are first-class, offline, same output formats so operators audit CI targets before --yes."],"topic":"CLI UX for cleaners: subcommands, table/JSON, exit codes, CI, progress"},{"anti_patterns":["Fake Your PC is N% dirty/infected gauges to drive action","Auto-clean on install, login, or schedule without prior user configuration","Freemium wall: free scan but paid delete or paid access to deletion logs","Blocking or mid-clean upsells to finish cleaning","Inflating reclaimable space with free space, other volumes, or unselected categories","Registry fix, startup optimizer, or RAM booster dressed as junk cleaning","Bundled PUPs, browser hijackers, or default-on affiliate installers","Deleting settings, passwords, or cookies under a cache or junk label","Hiding or paywalling deletion logs so users cannot audit what was removed","Reporting cleaned when files were only skipped or silently moved without saying so"],"must_have":["Preview/dry-run scan listing paths, categories, risk, and byte sizes before any delete","Clean command or flag separate from scan with interactive confirmation","Accurate per-finding, per-category, and total size aggregation","Post-clean summary with deleted count, bytes removed, and failures/skips with reasons","Persistent or exportable deletion log (human-readable and JSON) of what was deleted","Trash-first delete path with permanent delete only via explicit flag","Accurate rule labels/descriptions shipped with rule packs","No health score or scare UI in CLI output or documentation","Opt-in noninteractive mode that still writes full deletion logs","Medium/high-risk categories off by default until user opts in","Public docs of each category and what defaults never touch"],"rules":["Never invent PC dirty/health/threat scores or urgency not backed by concrete reclaimable paths.","Present scan results as facts only: bytes, paths, categories, risk, and rule IDs.","Default flow is preview then explicit user choice then confirm then clean; no silent first-run bulk delete.","Dry-run/preview is the safe default; never delete without preview or an explicit noninteractive consent flag.","Report measured reclaimable size on scan and attempted vs actually freed after clean; explain hard links, sparse files, and permission skips.","Never inflate sizes via double-counting, marketing up to claims, or non-deleted optimization theater.","Rule labels must match deleted content (e.g. logs must not wipe caches or settings).","Core clean path must work without accounts, licenses, nag screens, or paid unlock for real delete.","Every clean produces an accessible audit log or machine-readable report of deleted, failed, and skipped items with timestamps, sizes, and rule IDs.","Default to trash/recoverable delete; permanent delete only via explicit harder-to-reach option.","Interactive sessions confirm before delete; noninteractive requires explicit --yes/--force, never implied by scan.","No dark-pattern consent: no pre-checked high-risk wipe-all, buried extras, or fake progress inventing work.","Do not overclaim 100% safe, legal compliance theater, or malware removal unless those features truly exist and are separate.","No phone-home of path inventories or machine fingerprints without clear opt-in telemetry; default is no telemetry.","Report partial cleans honestly (permission denied, in-use files); never claim full success when results were partial."],"topic":"Legal/ethical product rules: no scareware, no forced upsell, honest sizes, accessible deletion logs"}]
```
